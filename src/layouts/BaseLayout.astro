---
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
}

const {
  title = 'Jiaqi Li - Academic Homepage',
  description = 'Jiaqi Li 的个人主页，展示教育经历、研究兴趣、项目与笔记。'
} = Astro.props;
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{title}</title>
    <link rel="icon" type="image/png" sizes="64x64" href="/images/new_favicon-64.png?v=1" />
    <script is:inline>
      (() => {
        const storedTheme = localStorage.getItem('theme');
        const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        document.documentElement.dataset.theme = storedTheme || (prefersDark ? 'dark' : 'light');
        document.documentElement.dataset.lang = localStorage.getItem('lang') || 'en';
      })();
    </script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@400;700;900&family=Noto+Serif+SC:wght@400;600;700&family=Noto+Sans+SC:wght@300;400;500;700&display=swap" rel="stylesheet" />
  </head>
  <body>
    <slot />
    <script>
      const themeToggle = document.querySelector('.theme-toggle');
      themeToggle?.addEventListener('click', () => {
        const nextTheme = document.documentElement.dataset.theme === 'dark' ? 'light' : 'dark';
        document.documentElement.dataset.theme = nextTheme;
        localStorage.setItem('theme', nextTheme);
      });

      const langToggle = document.querySelector('.lang-toggle');
      const syncLangToggle = () => {
        if (langToggle) {
          const currentLang = document.documentElement.dataset.lang === 'zh' ? 'zh' : 'en';
          langToggle.setAttribute('data-current', currentLang);
          langToggle.setAttribute('aria-label', currentLang === 'zh' ? '切换到英文' : 'Switch to Chinese');
        }
      };

      langToggle?.addEventListener('click', () => {
        const nextLang = document.documentElement.dataset.lang === 'zh' ? 'en' : 'zh';
        document.documentElement.dataset.lang = nextLang;
        localStorage.setItem('lang', nextLang);
        syncLangToggle();
      });
      syncLangToggle();

      const nav = document.querySelector('.nav');
      const backToTop = document.querySelector('.back-to-top');
      const sections = document.querySelectorAll('section[id]');
      const navLinks = document.querySelectorAll('.nav-links a');

      const updateScrollState = () => {
        const y = window.scrollY;
        nav?.classList.toggle('scrolled', y > 60);
        backToTop?.classList.toggle('visible', y > 400);

        let current = '';
        sections.forEach((section) => {
          const top = section.offsetTop - 120;
          if (y >= top) current = section.getAttribute('id') || '';
        });

        navLinks.forEach((link) => {
          link.classList.toggle('active', link.getAttribute('href') === `#${current}`);
        });
      };

      window.addEventListener('scroll', updateScrollState, { passive: true });
      updateScrollState();

      backToTop?.addEventListener('click', () => {
        window.scrollTo({ top: 0, behavior: 'smooth' });
      });

      const article = document.querySelector('#note-article');
      const toc = document.querySelector('#note-toc');
      if (article && toc) {
        const headings = [...article.querySelectorAll('h2, h3')];
        let tocLinks = [...toc.querySelectorAll('a')];

        if (tocLinks.length === 0) {
          const slugCounts = new Map();
          const toSlug = (value) => {
            const base = value
              .trim()
              .toLowerCase()
              .replace(/[^\p{L}\p{N}]+/gu, '-')
              .replace(/^-+|-+$/g, '') || 'section';
            const count = slugCounts.get(base) || 0;
            slugCounts.set(base, count + 1);
            return count === 0 ? base : `${base}-${count + 1}`;
          };

          headings.forEach((heading) => {
            if (!heading.id) heading.id = toSlug(heading.textContent || '');
          });

          toc.innerHTML = headings.map((heading) => (
            `<a href="#${heading.id}" data-id="${heading.id}" data-level="${heading.tagName === 'H3' ? '3' : '2'}">${heading.textContent}</a>`
          )).join('');
          tocLinks = [...toc.querySelectorAll('a')];
        }

        const setActive = (id) => {
          tocLinks.forEach((link) => {
            link.classList.toggle('active', link.dataset.id === id);
          });
        };

        const headingObserver = new IntersectionObserver((entries) => {
          const visible = entries
            .filter((entry) => entry.isIntersecting)
            .sort((a, b) => a.boundingClientRect.top - b.boundingClientRect.top)[0];
          if (visible?.target?.id) setActive(visible.target.id);
        }, {
          rootMargin: '-96px 0px -70% 0px',
          threshold: 0
        });

        headings.forEach((heading) => headingObserver.observe(heading));
        if (headings[0]) setActive(headings[0].id);
      }
    </script>
  </body>
</html>
