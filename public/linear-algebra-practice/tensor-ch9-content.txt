---
title: 第九章 张量
source: 高等代数导引
chapter: 9
tags: [高等代数, 张量, 线性泛函, 双线性型, Obsidian]
---

# 第九章 张量

> 说明：本文件按 PDF 第九章正文整理，去除了页眉页脚；公式、矩阵和推导尽量保留原书排版。遇到复杂公式时，另附的“版式保留备份”可用于对照。

## 目录

- 9.1 线性泛函
- 9.2 双线性型
- 9.3 张量的概念与表述
- 9.4 张量运算
- 9.5 作业

本章介绍线性泛函、双线性型，以及多线性型（张量），前二者当然也都是张量。在很大程度上这是线性泛函课程中部分内容的一个有限维版本。

在内积空间中，向量的坐标由它在基向量上的投影给出，在基变换下是逆变的，譬如某个基向量加长，则相应的坐标减小。另一方面，向量也可以由各基向量和它作内积得到的值来确定，称为余向量；这时，如果基向量加长，相应的内积是增大的，这表明它是协变的；而余向量也就给出了线性泛函：对于每个给定向量给出一个数值，且保持线性性。区分向量和泛函，更好的办法是放到一般的线性空间中，所有线性泛函按自然定义的加法和数乘构成了一个对偶空间。给定向量空间的基，对偶空间的每个基向量仅在其中一个（向量空间的）基向量上取值为1，其余均为0，这样的基称为对偶基。特别的，对偶空间的对偶空间本质上就是原来的线性空间。

如果对于线性空间给定的任意两个向量定义一个数值，且关于这两个向量都有线性性，那么这就是一个双线性型。在空间取定一组基后，双线性型就对应于一个度量阵，且双线性型的对称性、正定性、奇异性与度量阵的这些性质一一对应；特别的，当约束两个向量变量相等，双线性型就退化为二次型。

向量、双线性型、泛函是不变的，空间中的基变则向量坐标变、对偶基变、泛函在对偶基下的坐标变、表达双线性型的矩阵变、张量的各分量变。理解这段内容，一是回到如$\mathbb{R}^{3}$ 这样的具体有直观的例子中；二是通过推到多项式函数空间中理解其一般性。

张量的讨论离不开相应的记号。规定了空间中基、向量坐标、对偶基、泛函坐标，以及过渡阵和变换阵的上下标写法，并引入爱因斯坦约定（每一对相同的上下标为哑标，自动关于哑标在其取值范围内求和），张量的逆变、协变以及升降指标、张量积、点积等，就都很自然且简单了。张量作为多线性泛函可以形象地理解为一个翻牌子的函数，一个牌子（变量）若在向量空间中，张量关于它就相当于一个泛函的角色，是协变的；若一个

牌子是在对偶空间中，张量关于它就相当于一个向量的角色，是逆变的。如果每一个变量所在线性空间的基给出来了，对偶基当然也就确定了，那么要求恰好当这里每一个变量取某一个基或对偶基时值为1（其余为0）就给出了一个特殊的张量，这样的特殊张量通过线性组合可以给出所有的这样的一套牌子下的张量，于是称之为张量空间的一组基。一个给定张量在这组基下的坐标就称为张量坐标或张量分量。以上逆变协变是指相对于向量空间的坐标变化，张量如何变。

物理、力学中最重要的张量是所有变量都在同一个向量空间或其对偶空间中，通过一些例子，本章将给出基变换下张量坐标的变换公式，以及张量积、点积等运算。由于始终遵循基变换下向量、泛函、张量不变这一客观性原理，这些概念、运算就特别适用于物理和力学理论的阐释和发展。

最后，方阵的行列式是一个特殊的张量，它是各列（行）向量的交错化张量（外积）在选定基向量下的坐标。

## 9.1 线性泛函

函数是数集到数集的映射；如果像集在实数集里，称为（实）函数，如果在复数集里就称为复（变）函数；如果自变量在$\mathbb{R}^{n}$里，就称为n元函数；如果因变量在$\mathbb{R}^{n}$里，就称为向量值函数（如函数的梯度）。

线性代数考虑的自变量不局限在数集中，可以是一般的线性空间，这时，如果映射是线性的，就得到一个线性泛函。

**定义9.1.** $(V ; \mathbb{F})$为线性空间，称$f : V \to \mathbb{F}$为线性泛函(linear functional)，1若

$$
f(\lambda a + \mu b) = \lambda f(a) + \mu f(b),\forall a, b \in V, \lambda, \mu \in F.
$$

跟之前类似，本章仅讨论实线性泛函($\mathbb{F} = \mathbb{R}$).

**例9.1.** 在V = $\mathbb{R}^3$中，任取一个向量，如$b = (1, 2, 3)^\mathsf{T}$,<sup>2</sup>定义函数

$$
f(a) = a^1 \times 1 + a^2 \times 2 + a^3 \times 3 = (a, b)
$$

构成了一个线性泛函，这里$a = (a^{1}, a^{2}, a^{3})^\mathsf{T} \in \mathbb{R}^3$.

本章有些指标用上标表示，例如这里a的坐标，原因和规则后面解释。一般而言，对于内积中的两个变量，前一个向量的坐标用上标，后一个用下标。3

> 注1：也称线性函数或线性型(linear form).

> 注2：与内积空间的讨论类似，虽然本章通常不需要区分行向量和列向量，但仍按向量为列向量来表达。

> 注3：由于内积有对称性，偶尔混用上下标时意义也是清楚的。

**例9.2.** 在V = P<sub>3</sub>[x]中任一个多项式p([x]) = a<sup>3</sup>[x]<sup>3</sup> + a<sup>2</sup>[x]<sup>2</sup> + a<sup>1</sup>[x] + a<sup>0</sup>,定义 f(p([x])) = a<sup>3</sup> = (1/3!)$\cdot$(d<sup>3</sup>p/d[x]<sup>3</sup>)(0) 为线性泛函。4

**例9.3.** 对A $\in$V = $\mathbb{R}^{n \times n}$,定义f(A) = tr(A)，构成了一个线性泛函。

与迹不同，虽然行列式的值也是数，但det(A)涉及矩阵里元素的乘法，一般而言det(A + B) $\ne$  det(A) + det(B),不是线性泛函。

V 上所有线性泛函构成集合$L(V; \mathbb{F})$.在$L(V; \mathbb{F})$上定义

λf + µg : (λf + µg)(a) = λf(a) + µg(a),$\forall f, g \in L(V; \mathbb{F}), \forall a \in V,$

则$L(V; \mathbb{F})$ 构成线性空间。特别地，若dim V 有限，称$L(V; \mathbb{F})$ = $V^{*}$为V 的对偶空间。5

线性泛函最简单的例子是过原点的直线y = kx,而$V^{*}$就是除了y-轴以外所有这样的直线构成的空间，也就对应着k $\in \mathbb{R}$.

由特殊到一般，以下依次对$\mathbb{R}^n$,欧氏空间和一般的向量空间进行讨论。

在V = $\mathbb{R}^n$中由于线性性的要求，线性泛函必表示为(b<sub>1</sub>, · · ·, b<sub>n</sub> $\in \mathbb{R}$):

f(a) = b<sub>1</sub>a<sup>1</sup> + · · · + b<sub>n</sub>a<sup>n</sup>,$\forall a = (a^{1}, \cdots, a^{n}) ^\mathsf{T} \in \mathbb{R}^n$.

注意到$\mathbb{R}^n$中有自然定义的内积（构成欧氏空间E<sup>n</sup>）结构，视$b = (b_{1}, · · ·, b_{n})^\mathsf{T}$为一个向量，上述泛函可表示为内积或向量相乘：

$$
f(a) = (a, b) = a^Tb = b^Ta.
$$

这个观察构成本章内容的一个重要基础。

从定义出发，有以下一些结论：

1. $\forall b \in \mathbb{R}^n$,则f(a) = (a, b)定义了一个线性泛函；

2. $\forall f \in (\mathbb{R}^n)^{*}$, $\exists b \in \mathbb{R}^n$,使得f(a) = (a, b);

3. 上述从$(\mathbb{R}^n)^{*}$到$\mathbb{R}^n$的对应是1-1的；

4. 上述对应保持加法和数乘。

第1点：直接根据定义验算即可。

第2点：取

$$
b_i = f(e_i) \in R, (i = 1, \cdots, n),
$$

> 注4：这里为了区分上标与幂次，涉及幂次时x改写成了[x].

> 注5：无穷维赋范线性空间中，对偶空间定义为所有有界线性泛函构成的空间：而有限维空间上的线性泛函总是有界的。

这里{e<sub>1</sub>, · · ·, e<sub>n</sub>}为自然基。再令$b = (b_{1}, · · ·, b_{n})^\mathsf{T}$,由f的线性性

$$
f(a)= f(a^1e_1 + \cdots + a^ne_n)
 = a^1f(e_1) + \cdots + a^nf(e_n)
 = a^1b_1 + \cdots + a^nb_n
 = (a, b).
$$

第3点：记上述映射为

$$
\sigma :\mathbb{R}^n \to (\mathbb{R}^n)^{*}
 b \mapsto \sigma _b.
$$

一方面，显然b给出f = $\sigma$<sub>b</sub>定义了确定的线性泛函

$$
f(a) = (a, b),
$$

因此另一个线性泛函g $\ne$  f,必$\exists$i<sup>$\ast$</sup>, g(e<sup>$\ast$</sup>) $\ne$  f(e<sup>$\ast$</sup>) = b<sub>i$\ast$</sub>,从而g $\ne$  $\sigma$<sub>b</sub>.

另一方面，如果b $\ne$  c,则对于f = $\sigma$<sub>b</sub>, g = $\sigma$<sub>c</sub>，取a = b -c,

$$
f(a) -g(a) = (a, b) -(a, c)
 = (b -c, b -c)
 = \|b -c\|^2
 \neq 0.
$$

故f $\ne$  g.

综上，映射$\sigma$是1-1的。

第4点：由上面的取法，$\forall λ, µ \in \mathbb{R}$, f = $\sigma$<sub>b</sub>, g = $\sigma$<sub>c</sub>为线性泛函，记

$$
h = \sigma _\lambda b+\mu c,
$$

则$\forall a \in \mathbb{R}^n$,

$$
h(a)= (a, \lambda b + \mu c)
 = \lambda (a, b) + \mu (a, c)
 = (\lambda f + \mu g)(a),
$$

故

$$
\sigma _\lambda b+\mu c = \lambda \sigma _b + \mu \sigma _c,
$$

即$\sigma$保线性。

上述结论归纳起来，就是通过内积给出了同构$(\mathbb{R}^n)^{*}$$\sim$= $\mathbb{R}^n$.

在内积空间，结论类似。由于n维实内积空间V 与$\mathbb{R}^{n}$同构，前者内积为(·, ·),而后者内积标记为(·, ·)<sub>R</sub>.于是坐标向量的内积就是

$$
(x, y)_R = x^Ty.
$$

若a, b在基{$\varepsilon$<sub>1</sub>, · · ·, $\varepsilon$<sub>n</sub>}下的坐标分别是x, y,即

$$
a = [\varepsilon _1, \cdots, \varepsilon _n]x,b = [\varepsilon _1, \cdots, \varepsilon _n]y,
$$

如之前所述

$$
(a, b) = x^TGy,
$$

其中度量阵（Gram矩阵）

$$
G = \begin{pmatrix}
(\varepsilon_1, \varepsilon_1) & \cdots & (\varepsilon_1, \varepsilon_n) \\
\vdots & \ddots & \vdots \\
(\varepsilon_n, \varepsilon_1) & \cdots & (\varepsilon_n, \varepsilon_n)
\end{pmatrix}
\equiv
\begin{pmatrix} \varepsilon_1 \\ \vdots \\ \varepsilon_n \end{pmatrix}
[\varepsilon_1, \cdots, \varepsilon_n]
$$

标准正交基下，度量阵G为单位阵，于是V 中的内积与相应坐标向量的内积相等，即

$$
(a, b) = (x, y)_R.
$$

定理9.1 (里兹(Riesz)表示定理). V 为实内积空间，$\forall f \in V^{*}$, $\exists$唯一b $\in$V,成立

$$
f(a) = (a, b),\forall a \in V.
$$

证明. 记dim V = n,取V 的一组标准正交基{$\varepsilon$<sub>1</sub>, · · ·, $\varepsilon$<sub>n</sub>}.

取

$$
y_i = f(\varepsilon _i), (i = 1, \cdots, n),
$$

并定义b = y<sub>1</sub>$\varepsilon$<sub>1</sub> + · · · + y<sub>n</sub>$\varepsilon$<sub>n</sub>.

$\forall a \in V,$坐标为(x<sup>1</sup>, · · ·, x<sup>n</sup>)：

$$
f(a)= f(x^1\varepsilon _1 + \cdots + x^n\varepsilon _n)
 = x^1f(\varepsilon _1) + \cdots + x^nf(\varepsilon _n)
 = (x, y)_R
 = (a, b).
$$

另一方面，若另有c $\in$V,也成立f(a) = (a, c).则

$$
f(b -c)= (b -c, b)
 = (b -c, c),
$$

于是

$$
(b -c, b -c) = \|b -c\|^2 = 0,
$$

从而b -c = 0.故定理中的b唯一。■

与$\mathbb{R}^n$一样，内积空间中b \mapstof的映射也是1-1的且保线性，这就是说$V^{*}$$\sim$= V.其实

$$
V \cong \mathbb{R}^n \cong (\mathbb{R}^n)^{*}\cong V^{*}.
$$

容易知道：$\forall f \in V^{*}$成立f(a) = 0的充要条件是a = 0<sub>V</sub> ;若$\forall$a $\in$V 成立f(a) = 0,则称f为零泛函，就是$0_{V^\ast}$.

尽管V $\ast$与V 同构，一般而言它们之间是有区别的，这在V 作为内积空间上看不出来，因为内积的一个性质就是对称性，实内积空间V $\sim$= $V^{*}$.以下自然地给出b $\in V^{*}$的另一组基，就可以很方便地看出区别。<sup>6</sup>

里兹表示定理中用到了标准正交基下的度量阵为单位阵。对于一般的基向量，如果取

$$
b = [\varepsilon _1, \cdots, \varepsilon _n]G^{-1y},
$$

那么总有

$$
(a, b)= x^TG(G^{-1y})
 = (x, y)_R.
$$

该取法也可视作b(内积中第二个变量)按照另一组基

$$
[\varepsilon ^1, \cdots, \varepsilon ^n] = [\varepsilon _1, \cdots, \varepsilon _n]G^{-1}
$$

展开，其坐标仍为原来的基{$\varepsilon$<sub>1</sub>, · · ·, $\varepsilon$<sub>n</sub>}在给定泛函下的取值(y<sub>1</sub>, · · ·, y<sub>n</sub>)$^\mathsf{T}$.

直接计算：

$$
\begin{pmatrix}
(\varepsilon_1, \varepsilon^1) & \cdots & (\varepsilon_1, \varepsilon^n) \\
\vdots & \ddots & \vdots \\
(\varepsilon_n, \varepsilon^1) & \cdots & (\varepsilon_n, \varepsilon^n)
\end{pmatrix}
\equiv
\begin{pmatrix} \varepsilon_1 \\ \vdots \\ \varepsilon_n \end{pmatrix}
[\varepsilon^1, \cdots, \varepsilon^n]
=
\begin{pmatrix} \varepsilon_1 \\ \vdots \\ \varepsilon_n \end{pmatrix}
[\varepsilon_1, \cdots, \varepsilon_n] G^{-1}
= G G^{-1} = I_n.
$$

> 注6：如果视(·, b)为一个泛函，它属于V $\ast$,简称为b $\in$V $\ast$(此时约定内积中第二个向量即余向量);但实际上b当然也是V 中的向量。严格地说,b $\in$V 而(·, b) $\in$V $\ast$,但在内积空间中这样区分它们并没有太大意义，所以会说：把b视为向量就是V 中的，视为余向量就是V $\ast$中的。

于是

$$
(\varepsilon _i, \varepsilon ^j) = \delta ^j_i.^7
$$

按照分量形式重新证明里兹表示定理。记

a = x<sup>1</sup>$\varepsilon$<sub>1</sub> + · · · + x<sup>n</sup>$\varepsilon$<sub>n</sub>,b = y<sub>1</sub>$\varepsilon$<sup>1</sup> + · · · + y<sub>n</sub>$\varepsilon$<sup>n</sup>,

则

$$
\begin{aligned}
f(a) &= x^1 f(\varepsilon_1) + \cdots + x^n f(\varepsilon_n) \\
 &= \sum_{i=1}^{n} x^i y_i \\
 &= \sum_{i,j=1}^{n} x^i y_j \delta^j_i \\
 &= \sum_{i,j=1}^{n} x^i y_j (\varepsilon_i, \varepsilon^j) \\
 &= \left(\sum_{i=1}^{n} x^i \varepsilon_i,\ \sum_{j=1}^{n} y_j \varepsilon^j\right) \\
 &= (a, b).
\end{aligned}
$$

以下讨论坐标变换，需谨记向量和泛函不变。

考虑基变换

$$
(\tilde \varepsilon _1, \cdots, \tilde \varepsilon _n) = (\varepsilon _1, \cdots, \varepsilon _n)T,
$$

这里T = (t<sup>i</sup><sub>j</sub>)<sub>n$\times$n</sub>(行标在上，列标在下)。用爱因斯坦约定（一对上下标相同则作为哑标，在其取值范围内求和）就是

$$
\tilde \varepsilon _j = t^i_j\varepsilon _i.
$$

如同线性空间一章讨论过的，若变换前后坐标分别为x, $\tilde{x}$,则

$$
a= (\varepsilon _1, \cdots, \varepsilon _n)x
 = (\tilde \varepsilon _1, \cdots, \tilde \varepsilon _n)\tilde x
 = (\varepsilon _1, \cdots, \varepsilon _n)T \tilde x,
$$

因此，变换后a 的坐标向量为

$$
\tilde x = T^{-1}x.
$$

> 注7：就是以前定义的克罗涅克德尔塔。ji=1[x]=0

再看对偶空间$V^{*}$，设基变换的过渡矩阵为S,此时根据

$$
I_n =
\begin{pmatrix} \tilde\varepsilon_1 \\ \vdots \\ \tilde\varepsilon_n \end{pmatrix}
[\tilde\varepsilon^1, \cdots, \tilde\varepsilon^n]
=
T^{\mathsf{T}}
\begin{pmatrix} \varepsilon_1 \\ \vdots \\ \varepsilon_n \end{pmatrix}
[\varepsilon^1, \cdots, \varepsilon^n] S
= T^{\mathsf{T}} S,
$$

因此，S = T<sup>-T</sup>,即对偶空间基变为

$$
(\tilde \varepsilon ^1, \cdots, \tilde \varepsilon ^n) = (\varepsilon ^1, \cdots, \varepsilon ^n)T^{-T},
$$

此时,b的坐标为

$$
\tilde y = S^{-1y} = T^{\mathsf{T}}y.
$$

从而

$$
(\tilde x, \tilde y)_R = (T^{-1}x)^T(T^{\mathsf{T}}y) = x^Ty = (x, y)_R,
$$

定义了同一个内积(a, b),从而定义了同一个线性泛函。

这两种坐标变换的不同，在张量的讨论中将称V 中的变量a为逆变的,$V^{*}$中的变量（泛函f或作为其内积表示的余向量b）为协变的。

**例9.4.** $\mathbb{R}^3$中取自然基{e<sub>1</sub>, e<sub>2</sub>, e<sub>3</sub>}.

1. 计算对偶基；若向量a在自然基下坐标为$x = (1, 2, 3)^\mathsf{T}$，对偶基下某线性泛函f的坐标向量为$y = (2, 3, 4)^\mathsf{T}$, 求f(a).

2. 若另选一组基

$$
\varepsilon _1 = (1, 0, 0)^T, \varepsilon _2 = (1, 1, 0)^T, \varepsilon _3 = (1, 1, 1)^T,
$$

求a在这组基下的坐标向量$\tilde{x}$;计算这组基的对偶基{$\varepsilon$<sup>1</sup>, $\varepsilon$<sup>2</sup>, $\varepsilon$<sup>3</sup>},以及f新的坐标向量$\tilde{y}$；并验算f(a)不变。

解. 1. 因为自然基是标准正交基，其度量阵为I<sub>n</sub>,故对偶基与自然基相等，即为

$$
e^1 = (1, 0, 0)^T, e^2 = (0, 1, 0)^T, e^3 = (0, 0, 1)^T.
$$

记f相应的内积表示中向量为

$$
b = (e^1, e^2, e^3)y = (2, 3, 4)^T,
$$

则

$$
f(a) = (a, b) = 20.
$$

2. 基{$\varepsilon$<sub>1</sub>, $\varepsilon$<sub>2</sub>, $\varepsilon$<sub>3</sub>}的过渡阵由它们的坐标向量组装而成

$$
T = \begin{pmatrix}
1 & 1 & 1 \\
0 & 1 & 1 \\
0 & 0 & 1
\end{pmatrix},\quad
T^{-1} = \begin{pmatrix}
1 & -1 & 0 \\
0 & 1 & -1 \\
0 & 0 & 1
\end{pmatrix}
$$

于是a的新的坐标向量为

$$
\tilde{x} = T^{-1} x = \begin{pmatrix}
1 & -1 & 0 \\
0 & 1 & -1 \\
0 & 0 & 1
\end{pmatrix}
\begin{pmatrix} 1 \\ 2 \\ 3 \end{pmatrix}
= \begin{pmatrix} -1 \\ -1 \\ 3 \end{pmatrix}.
$$

可以验算

$$
(-1) \times \begin{pmatrix} 1 \\ 0 \\ 0 \end{pmatrix}
+ (-1) \times \begin{pmatrix} 1 \\ 1 \\ 0 \end{pmatrix}
+ 3 \times \begin{pmatrix} 1 \\ 1 \\ 1 \end{pmatrix}
= \begin{pmatrix} 1 \\ 2 \\ 3 \end{pmatrix}.
$$

另一方面，根据对偶基的定义，应该有

$$
(\varepsilon _1, \varepsilon ^1) = (1, 0, 0)\varepsilon ^1 = 1,
 (\varepsilon _2, \varepsilon ^1) = (1, 1, 0)\varepsilon ^1 = 0,
 (\varepsilon _3, \varepsilon ^1) = (1, 1, 1)\varepsilon ^1 = 0,
$$

解出$\varepsilon$<sup>1</sup> = (1, -1, 0)$^\mathsf{T}$;类似地，从

$$
(1, 0, 0)\varepsilon ^2 = 0, (1, 1, 0)\varepsilon ^2 = 1, (1, 1, 1)\varepsilon ^2 = 0,
 (1, 0, 0)\varepsilon ^3 = 0, (1, 1, 0)\varepsilon ^3 = 0, (1, 1, 1)\varepsilon ^3 = 1
$$

分别解出$\varepsilon$<sup>2</sup> = (0, 1, -1)$^\mathsf{T}$, $\varepsilon$<sup>3</sup> = (0, 0, 1)$^\mathsf{T}$.

其实，把它们排成矩阵就是

$$
[\varepsilon^1, \varepsilon^2, \varepsilon^3] = \begin{pmatrix}
1 & 0 & 0 \\
-1 & 1 & 0 \\
0 & -1 & 1
\end{pmatrix}
= [e^1, e^2, e^3] T^{-\mathsf{T}}.
$$

于是，f内积表示中的向量b的坐标为

$$
\eta = \begin{pmatrix}
1 & 0 & 0 \\
-1 & 1 & 0 \\
0 & -1 & 1
\end{pmatrix} y
= T^{\mathsf{T}} y
= \begin{pmatrix}
1 & 0 & 0 \\
1 & 1 & 0 \\
1 & 1 & 1
\end{pmatrix}
\begin{pmatrix} 2 \\ 3 \\ 4 \end{pmatrix}
= \begin{pmatrix} 2 \\ 5 \\ 9 \end{pmatrix}.
$$

同样可以验证

$$
2 \times \begin{pmatrix} 1 \\ -1 \\ 0 \end{pmatrix}
+ 5 \times \begin{pmatrix} 0 \\ 1 \\ -1 \end{pmatrix}
+ 9 \times \begin{pmatrix} 0 \\ 0 \\ 1 \end{pmatrix}
= \begin{pmatrix} 2 \\ 3 \\ 4 \end{pmatrix}.
$$

于是，再计算

$$
f(a) = (\xi, \eta)_{\mathbb{R}}
= (-1, -1, 3) \begin{pmatrix} 2 \\ 5 \\ 9 \end{pmatrix}
= 20.
$$

在上面的计算中，也可以通过计算{$\varepsilon$<sub>1</sub>, $\varepsilon$<sub>2</sub>, $\varepsilon$<sub>3</sub>}的度量阵

$$
G = \begin{pmatrix}
(\varepsilon_1, \varepsilon_1) & (\varepsilon_1, \varepsilon_2) & (\varepsilon_1, \varepsilon_3) \\
(\varepsilon_2, \varepsilon_1) & (\varepsilon_2, \varepsilon_2) & (\varepsilon_2, \varepsilon_3) \\
(\varepsilon_3, \varepsilon_1) & (\varepsilon_3, \varepsilon_2) & (\varepsilon_3, \varepsilon_3)
\end{pmatrix}
= \begin{pmatrix}
1 & 1 & 1 \\
1 & 2 & 2 \\
1 & 2 & 3
\end{pmatrix},
$$

及其逆矩阵

$$
G^{-1} = \begin{pmatrix}
2 & -1 & 0 \\
-1 & 2 & -1 \\
0 & -1 & 1
\end{pmatrix},
$$

得到

$$
\begin{pmatrix}
1 & 1 & 1 \\
0 & 1 & 1 \\
0 & 0 & 1
\end{pmatrix}
\begin{pmatrix}
2 & -1 & 0 \\
-1 & 2 & -1 \\
0 & -1 & 1
\end{pmatrix}
= \begin{pmatrix}
1 & 0 & 0 \\
-1 & 1 & 0 \\
0 & -1 & 1
\end{pmatrix}
= [\varepsilon^1, \varepsilon^2, \varepsilon^3].
$$

此外，$\xi$也可由a分别与$\varepsilon$<sup>1</sup>, $\varepsilon$<sup>2</sup>, $\varepsilon$<sup>3</sup>作内积得到其各个分量；$\eta$也可由b分别与$\varepsilon$<sub>1</sub>, $\varepsilon$<sub>2</sub>, $\varepsilon$<sub>3</sub>作内积得到其各个分量。

最后，讨论一般（不假定有内积结构）的有限维空间V 上的线性泛函。

断言dim $V^{*}$= dim V.基本的证明思路仍是取$V^{*}$的基，只是不能再用内积空间的语言。

设V 有一组基{$\varepsilon$<sub>1</sub>, · · ·, $\varepsilon$<sub>n</sub>},构造以下一组线性泛函：

$\forall i = 1, \cdots, n, \forall a = x^{1}ε_{1} + \cdots + x^{n}ε_{n} \in V,$定义

$$
\varepsilon ^i(a) = x^i.
$$

容易验证εi为V 上的线性泛函，且

$$
\varepsilon ^i(\varepsilon _j) = \delta ^i_j,j = 1, \cdots, n.
$$

{$\varepsilon$<sup>1</sup>, · · ·, $\varepsilon$<sup>n</sup>}构成了$V^{*}$的一组基，证明如下。

1. $\forall f \in V,$记y<sub>i</sub> = f(e<sub>i</sub>),则$\forall a = x^{1}ε_{1} + \cdots + x^{n}ε_{n} \in V$

$$
f(a)= f(x^1\varepsilon _1 + \cdots + x^n\varepsilon _n)
 = x^1f(\varepsilon _1) + \cdots + x^nf(\varepsilon _n)
 = x^1y_1 + \cdots + x^ny_n
 = y_1\varepsilon ^1(a) + \cdots + y_n\varepsilon ^n(a),
$$

这就是说

$$
f = y_1\varepsilon ^1 + \cdots + y_n\varepsilon ^n,
$$

即可以由{$\varepsilon$<sup>1</sup>, · · ·, $\varepsilon$<sup>n</sup>}线性表出。$\sum$n

2. 设 $\lambda$<sub>i</sub>$\varepsilon$<sup>i</sup> = 0,则对j = 1, · · ·, n计算i=1

$$
\sum_{i=1}^{n}
 0= \lambda _i\varepsilon ^i(\varepsilon _j)
\\sum_{i=1}^{n}
 = \lambda _i\delta ^i

= \lambda _j.
$$

这表明{$\varepsilon$<sup>1</sup>, · · ·, $\varepsilon$<sup>n</sup>}线性无关。

称{$\varepsilon$<sup>1</sup>, · · ·, $\varepsilon$<sup>n</sup>}为{$\varepsilon$<sub>1</sub>, · · ·, $\varepsilon$<sub>n</sub>}的一组对偶基(dual basis).

对偶基的性质$\varepsilon$<sup>i</sup>($\varepsilon$<sub>j</sub>) = $\delta$<sup>i</sup> <sub>j</sub>表明，用V 中的基可以探明$V^{*}$中的泛函：$\forall f \in V^{*}$,

$$
f = f(\varepsilon _1)\varepsilon ^1 + \cdots + f(\varepsilon _n)\varepsilon ^n;
$$

而用$V^{*}$中的基可以探明V 中的向量：$\forall$a $\in$V,

$$
a = \varepsilon ^1(a)\varepsilon _1 + \cdots + \varepsilon ^n(a)\varepsilon _n.
$$

后者也称为广义傅里叶分解。而其基础在于：由上述表达式看出a = 0<sub>V</sub> 的充要条件是$\forall f \in V^{*}$成立f(a) = 0;而根据定义f = $0_{V^\ast}$的充要条件是$\forall$a $\in$ V 成立f(a) = 0.

向量与线性泛函分别在基和对偶基下分解后，若记坐标向量为$x = (x^{1}, · · ·, x^{n})^\mathsf{T}$, $y = (y_{1}, · · ·, y_{n})^\mathsf{T}$,则取值为

$$
f(a) = (x, y)_R = x^iy_i,
$$

这里再次用了爱因斯坦约定，以后不再解释。

基于与内积空间的相似性，有时候我们也记

$$
f(a) =< a, f >,
$$

那么上面的结论就是：

$$
a =< a, \varepsilon ^i > \varepsilon _i, f =< \varepsilon _i, f > \varepsilon ^i.
$$

利用这种记号可以看出来内积空间中基变换的所有公式依然成立。以对偶基的变换为例，若V 中的基变换为

$$
[\tilde \varepsilon _1, \cdots, \tilde \varepsilon _n] = [\varepsilon _1, \cdots, \varepsilon _n]T,
$$

设相应的对偶基变换为

$$
[\tilde \varepsilon ^1, \cdots, \tilde \varepsilon ^n] = [\varepsilon ^1, \cdots, \varepsilon ^n]S,
$$

则根据对偶基与基之间的关系

$$
I_n =
\begin{pmatrix}
\langle \varepsilon_1 \rangle \\
\vdots \\
\langle \varepsilon_n \rangle
\end{pmatrix}
[\varepsilon^1, \cdots, \varepsilon^n],
$$

这里矩阵的含义与内积空间中类似，即第(i, j)元为$\langle$$\varepsilon$<sub>i</sub>, $\varepsilon$<sup>j</sup>$\rangle$.

变换后仍应有

$$
I_n =
\begin{pmatrix}
\langle \tilde\varepsilon_1 \rangle \\
\vdots \\
\langle \tilde\varepsilon_n \rangle
\end{pmatrix}
[\tilde\varepsilon^1, \cdots, \tilde\varepsilon^n],
$$

而从变换知

$$
\begin{pmatrix}
\langle \tilde\varepsilon_1 \rangle \\
\vdots \\
\langle \tilde\varepsilon_n \rangle
\end{pmatrix}
[\tilde\varepsilon^1, \cdots, \tilde\varepsilon^n]
= T^{\mathsf{T}}
\begin{pmatrix}
\langle \varepsilon_1 \rangle \\
\vdots \\
\langle \varepsilon_n \rangle
\end{pmatrix}
[\varepsilon^1, \cdots, \varepsilon^n] S
= T^{\mathsf{T}} S,
$$

于是

$$
S = T^{-T}.
$$

**例9.5.** 在多项式空间P<sub>n</sub>[x]上，分别求以下两种基的对偶基(以下i, $j \in {0$, · · ·, n})：

1. {$\varepsilon$<sub>0</sub>, · · ·, $\varepsilon$<sub>n</sub>}为一组基，其中$\varepsilon$<sub>j</sub> = [x]<sup>j</sup>；

2. 先取定n + 1个不同的点(x<sub>0</sub>, x<sub>1</sub>, · · ·, x<sub>n</sub>),定义<sup>8</sup>

$\tilde{varepsilon}$<sub>i</sub>([x]) = ([x] -x<sub>0</sub>) · · ·([x] -x<sub>i</sub>) · · · ([x] -x<sub>n</sub>)\

$$
,
$$

(x<sub>i</sub> -x<sub>0</sub>) · · ·(x<sub>i</sub> -x<sub>i</sub>) · · · (x<sub>i</sub> -x<sub>n</sub>)\

以{$\tilde{varepsilon}$<sub>0</sub>, · · ·, $\tilde{varepsilon}$<sub>n</sub>}为一组基。

解. 1. 由对偶基的定义，$\varepsilon$<sup>i</sup>([x]<sup>j</sup>) = $\delta$<sup>i</sup> <sub>j</sub>，于是对于任一个多项式

$$
p([x]) = a_0 + a_1[x] + \cdots + a_n[x]^n,
$$

$\varepsilon$<sup>i</sup>(p([x]))= $\varepsilon$<sup>i</sup>(a<sub>0</sub> + a<sub>1</sub>[x] + · · · + a<sub>n</sub>[x]<sup>n</sup>)

$$
= a_0\varepsilon ^i(1) + a_1\varepsilon ^i([x]) + \cdots + a_n\varepsilon ^i([x]^n)
 = a_i
 = 1d^i
 d[x]_i p([0]).
$$

i!因此，对偶基($\varepsilon$<sup>0</sup>, · · ·, $\varepsilon$<sup>n</sup>)可用算子形式表示为（因d<sup>i</sup> d[x]<sub>i</sub> 中的i表示幂次，不是指标，因而不关于i求和）

$$
\varepsilon i = 1d^i
 .
$$

i!d[x]i

> 注8：c表示去掉该项因式。j

2. 设任一多项式可展开成

$$
p([x]) = y_0 \tilde \varepsilon _0([x]) + \cdots + y_n \tilde \varepsilon _n([x]),
$$

令[x] = x<sub>i</sub>得

$$
p(x_i) = y_i.
$$

再看对偶基{$\tilde{varepsilon}$<sup>0</sup>, · · ·, $\tilde{varepsilon}$<sup>n</sup>},应满足

$$
\tilde \varepsilon ^i(\tilde \varepsilon _j([x])) = \delta ^i_j,
$$

于是

$$
\tilde \varepsilon ^i(p([x]))= \tilde \varepsilon ^i(y_0 \tilde \varepsilon _0([x]) + \cdots + y_n \tilde \varepsilon _n([x]))
 = y_0 \tilde \varepsilon ^i(\tilde \varepsilon _0([x])) + \cdots + y_n \tilde \varepsilon ^i(\tilde \varepsilon _n([x]))
 = y_i
 = p(x_i).
$$

即对偶基就是在选定点的取值泛函。

上面的第二组基给出了拉格朗日插值多项式。基虽然复杂一点，但对偶基很简单。特别的，如果已知一个函数在给定点处的值，就可以用这些值乘以相应的拉格朗日插值多项式相加后，得到给定函数的一个多项式近似表达式。

对于多项式中基的变换：

$$
= ([x] -x_0) \cdots([x] -x_i) \cdots ([x] -x_n)\
$$

$\tilde{varepsilon}$<sub>i</sub> (x<sub>i</sub> -x<sub>0</sub>) · · ·(x<sub>i</sub> -x<sub>i</sub>) · · · (x<sub>i</sub> -x<sub>n</sub>)\

$$
\sum_{\\sum_{i=1}^{n}}^{(-x_s1) \cdots (-x_sℓ)}
 0≤s1<\cdots<sℓ≤n,且\neq i [x]^ℓ,
 =
$$

(x<sub>i</sub> -x<sub>0</sub>) · · ·(x<sub>i</sub> -x<sub>i</sub>) · · · (x<sub>i</sub> -x<sub>n</sub>)\

$$
ℓ=0
$$

过渡阵形如

$$
(-x_1) \cdots (-x_n) (-x_0) \cdots (-x_n-1)
 \cdots
$$

(x<sub>0</sub> -x<sub>1</sub>) · · · (x<sub>0</sub> -x<sub>n</sub>) (x<sub>0</sub> -x<sub>1</sub>) · · · (x<sub>0</sub> -x<sub>n</sub>)

$$
\cdots \cdots
$$

T =   , 1    1

$$
\cdots
$$

(x<sub>n</sub> -x<sub>0</sub>) · · · (x<sub>n</sub> -x<sub>n-1</sub>) (x<sub>n</sub> -x<sub>0</sub>) · · · (x<sub>n</sub> -x<sub>n-1</sub>)

再看对偶基的变换：

$$
\tilde \varepsilon ^j(p([x]))= p(x_j)
 = a_mx^m

= x^m_j \varepsilon ^m(p([x])).
$$

过渡阵形如


1· · ·x<sup>n</sup>

$$
0,
 \cdots
 S =
$$

1· · ·x<sup>n</sup> n就是范德蒙方阵的转置。顺带知道T 就是范德蒙方阵ST的逆矩阵。

以上讨论了V 上线性泛函构成的对偶空间$V^{*}$.$V^{*}$作为一个相同维数的线性空间，还可以定义它的对偶空间

$$
V^{*}∗\equiv (V^{*})^{*}.
$$

但实际上，有一个自然同构:<sup>9</sup>

$$
\tau : V \to V^{*}∗,
$$

对于给定的a $\in$V,a<sup>$\ast$$\ast$</sup>= $\tau$<sub>a</sub> $\in$V<sup>$\ast$$\ast$</sup>定义为:$\forall f \in V^{*}$,

$$
a^{*}∗(f) = f(a).
$$

首先，$\tau$是单射。这是因为$\forall a \neq  ˜a,即a −˜a \neq  0,那么\exists f \in V^{*}$, 使得

$$
f(a -\tilde a) \neq 0,
$$

即

$$
f(a) \neq f(\tilde a),
$$

亦即

$$
a^{*}∗(f) \neq \tilde a^{*}∗(f),
$$

于是

$$
a^{*}∗\neq \tilde a^{*}∗,
$$

这就是

$$
\tau _a \neq \tau _\tilde a.
$$

另一方面，$\tau$是满射。事实上，$\forall a^{**} \in V^{**}.$记它在相应于V 中的基{$\varepsilon$<sub>1</sub>, · · ·, $\varepsilon$<sub>n</sub>}的对偶基{$\varepsilon$<sup>1</sup>, · · ·, $\varepsilon$<sup>n</sup>} 上的取值依次为

$$
x^1 = a^{*}∗(\varepsilon ^1), \cdots, x^n = a^{*}∗(\varepsilon ^n),
$$

这就是说

$$
\varepsilon ^1(a) = x^1,\cdots, \varepsilon ^n(a) = x^n.
$$

> 注9：这并非V 到V $\ast$$\ast$上唯一的同构，例如a$\ast$$\ast$(f) = f(3a)也定义了一个同构。但同构$\tau$是最自然的。

定义

$$
a = x^i\varepsilon _i,
$$

那么,$\forall$f = y<sub>j</sub>$\varepsilon$<sup>j</sup>,

$$
\tau _a(f)= f(a)
 = (y_j\varepsilon ^j)(x^i\varepsilon _i)
 = y_j(x^i\varepsilon ^j(\varepsilon _i))
 = y_ja^{*}∗(\varepsilon ^j)
 = a^{*}∗(y_j\varepsilon ^j)
 = a^{*}∗(f).
$$

此外，$\tau$保线性，从上面已经看到$\forall λ, µ \in \mathbb{R}$, a, $\tilde{a}$ $\in$V, f $\in V^{*}$,

$$
\tau _\lambda a+\mu \tilde a(f)= f(\lambda a + \mu \tilde a)
 = \lambda f(a) + \mu f(\tilde a)
 = (\lambda \tau _a + \mu \tau _\tilde a)(f),
$$

即

$$
\tau _\lambda a+\mu \tilde a = \lambda \tau _a + \mu \tau _\tilde a.
$$

这就证明了V 和V $\ast$$\ast$之间有自然同构。简单地说，对偶空间的对偶空间就是原空间。

## 9.2 双线性型

线性泛函对于每个向量给出一个数值，双线性型则对（在同一个空间里）每两个向量给出一个数值。

**定义9.2.** V 为F上的n维线性空间，若$f : V \times V \to \mathbb{F}$关于两个元都线性，即

$$
f(\lambda a + \mu \tilde a, b)= \lambda f(a, b) + \mu f(\tilde a, b),
 f(a, \lambda b + \mu \tilde b)= \lambda f(a, b) + \mu f(a, \tilde b).
$$

则称之为V 上的双线性型(bilinear form)，也称为双线性函数。

**例9.6.** 内积空间V 上的内积f(a, b) = (a, b)为双线性型。

**例9.7.** 若g, h $\in V^{*}$,定义f(a, b) = g(a)h(b)为双线性型。<sup>10</sup>

**例9.8.** A为n阶方阵，$\forall a, b \in \mathbb{R}^n$,定义f(a, b) = a$^\mathsf{T}$Ab给出了一个双线性型。

> 注10：这就是下一节中的 g $\otimes$ h.

前面说过，矩阵的行列式不是线性泛函，但如果把二阶矩阵看成两个列向量，情况就不同了。

**例9.9.** 对a, b $\in$V = $\mathbb{R}^2$,定义f(a, b) = det (a|b),构成了一个双线性型。

跟之前一样，考虑所有V 上的双线性型构成的集合$L(V \times V, \mathbb{R})$.在V 上取定一组基{e<sub>1</sub>, · · ·, e<sub>n</sub>}, 对于任一双线性型f,由于$\forall a, b \in V,$可用坐标表示为a = x<sup>1</sup>e<sub>1</sub> + · · · + x<sup>n</sup>e<sub>n</sub>,b = y<sup>1</sup>e<sub>1</sub> + · · · + y<sup>n</sup>e<sub>n</sub>.

再记矩阵（按照行标在上、列标在下的记号）

$$
A = (a^i_j)_n\times n, 其中a^i_j = f(e_i, e_j).
$$

那么

$$
f(a, b)= f (x^ie_i, y^je_j)
 = x^iy^jf(e_i, e_j)
 = x^iy^ja^i
$$

j

$$
= (x^1, \cdots, x^n)A(y^1, \cdots, y^n)^T
 = x^TAy.
$$

值得特别指出的是，两个i都在上标不符合爱因斯坦约定，这相应于x的转置（xi在x中为第i行，在xT中就成了第i列）。

这样的方阵A称为双线性型f在基{e<sub>1</sub>, · · ·, e<sub>n</sub>}下的度量阵。内积空间里的度量阵是其特例。

双线性型和n阶方阵之间构成了一个同构（证明留给读者），套用前面的记号就是

$$
L(V \times V, \mathbb{R}) \cong \mathbb{R}^n\times n.
$$

如果按以下方式定义双线性型：

$$
\psi ^pq(\varepsilon _i, \varepsilon _j) = \delta ^p_i \delta ^q_j,i, j = 1, \cdots, n,
$$

即变换阵仅(p, q)元等于1，其它皆为0,那么，一方面，任何一个双线性型必可表示为这些（p, q = 1, · · ·, n）双线性型的线性组合，即度量阵为A = (a<sub>ij</sub>)的双线性型表示为

$$
f = a_pq\psi ^pq;
$$

另一方面，容易知道这些双线性型线性无关。于是，{$\psi$<sup>pq</sup>|p, q = 1, · · ·, n}构成$L(V \times V, \mathbb{R})$的一组基。

此外，利用对偶基的概念，上面的定义就是

$$
\psi ^pq(\varepsilon _i, \varepsilon _j) = \varepsilon ^p(\varepsilon _i)\varepsilon ^q(\varepsilon _j),
$$

记为

$$
\varepsilon ^p ⊗\varepsilon ^q(\varepsilon _i, \varepsilon _j) \equiv \varepsilon ^p(\varepsilon _i)\varepsilon ^q(\varepsilon _j),
$$

这就是说，

$$
\psi ^pq = \varepsilon ^p ⊗\varepsilon ^q,
$$

而用这些双线性型张成的线性空间（即上面所有的f）自然地就记为$V^{*}$$\otimes V^{*}$,这样

$$
L(V \times V, \mathbb{R}) = V^{*}⊗V^{*}.
$$

如果V 换一组基

$$
(\varepsilon _1, \cdots, \varepsilon _n) = (e_1, \cdots, e_n)T,
$$

那么计算可得

$$
()
 f(\varepsilon _i, \varepsilon _j)= ft^ℓ_ie_ℓ, t^m_j e_m
 = t^ℓ_it^m_j f(e_ℓ, e_m)
 = t^ℓ_it^m_j a^ℓ_m,
$$

这就是说，新的度量阵为

$$
A = T^{\mathsf{T}}AT.\tilde
$$

因此基变换前后的度量阵相合。转置在这里表现为$\ell$的求和中，在t<sup>$\ell$</sup>    <sub>i</sub>, a<sup>$\ell$</sup><sub>m</sub>中都是上标，而f($\varepsilon$<sub>i</sub>, $\varepsilon$<sub>j</sub>)作为度量阵为$\tilde{a}$<sup>i</sup> <sub>j</sub>, 其中i为上标，与上述求和之后i落在下标，上下反了。若考虑($\Theta$) = T$^\mathsf{T}$,其$\theta$<sup>i</sup> <sub>$\ell$</sub>= t<sup>$\ell$</sup><sub>i</sub>,有

$$
\tilde a^i_j = \theta ^i_ℓt^m_j a^ℓ_m,
$$

指标就都是顺的了。

现在引入新记号

$$
< a, b >_f= f(a, b).
$$

那么，在V 的基{e<sub>1</sub>, · · ·, e<sub>n</sub>}下

$$
a = (e_1, \cdots, e_n)x,b = (e_1, \cdots, e_n)y,
$$

于是


$\langle$ e<sub>1</sub>    $\rangle$

$$
\cdots, [e_1, \cdots, e_n]y
 < a, b >_f =x^T
$$

e<sub>n</sub>    f


$\langle$ e<sub>1</sub>    $\rangle$

$$
\cdots, [e_1, \cdots, e_n]
 = x^T y
$$

e<sub>n</sub>

$$
= x^TAy.
$$

这与内积的讨论完全“平行”，基变换下内积的度量阵之变换仅用到内积的双线性性，因而在这里同样成立。

无论采用哪一种讨论，基本出发点在于对给定的两个向量，无论基怎么变（因为向量不变，它们的坐标改变），双线性型（即在这两个向量处的取值）不变，从而其度量阵发生改变。

**定义9.3.** 称f为非退化双线性型，若：$\forall b \in V, f(a, b) = 0 ⇒a = 0; \forall a \in V, f(a, b) = 0 ⇒b = 0.$

从双线性型的矩阵表示知道，f非退化的等价条件是度量阵非奇异。注意这与基选择无关，因为不同基下的度量阵相合、从而相抵。

**定义9.4.** 称f为对称双线性型，若$\forall a, b \in V, f(a, b) = f(b, a);$称f为反对称双线性型，若$\forall a, b \in V, f(a, b) = −f(b, a).$

仍从双线性型的矩阵表示知道，双线性型对称当且仅当度量阵对称；双线性型反对称当且仅当度量阵反对称。

**定理9.2.** 若f为线性空间V 上的对称双线性型，则$\exists$p, m,可选一组基，f在这组基下的度量阵为形如diag(I<sub>p</sub>, -I<sub>m</sub>, O) 的对角阵。

证明. （法一）设f在某组基{e<sub>1</sub>, · · ·, e<sub>n</sub>}下表示为实对称阵A,由惯性定理A相合于对角阵，即$\exists$T 可逆，

$$
T AT^{\mathsf{T}} = diag(I_p, -I_m, O).
$$

取V 的基($\varepsilon$<sub>1</sub>, · · ·, $\varepsilon$<sub>n</sub>) = (e<sub>1</sub>, · · ·, e<sub>n</sub>)T$^\mathsf{T}$,则在这组基下f的矩阵表示为

$$
A = (T^{\mathsf{T}})^TAT^{\mathsf{T}} = diag(I_p, -I_m, O).\tilde
$$

（法二）用构造性的方法给出定理另一种证明。

若dim V = 1,此时任选一组基e<sub>1</sub>，度量阵A = (a<sup>1</sup>    <sub>1</sub>)本身就是对角$\sqrt$形，若a<sup>1</sup><sub>1</sub> = 0,则定理结论成立；否则取$\varepsilon$<sub>1</sub> = e<sub>1</sub>/    |a<sup>1</sup><sub>1</sub>|,此时f的矩阵表示为(sign(a<sup>1</sup><sub>1</sub>)I<sub>1</sub>).

设dim V = n -1时定理结论成立。考虑dim V = n,$\forall$f 为V 上的双线性型。

1. 若$\forall a, b \in V$，成立f(a, b) = 0，则f $\equiv$0,任取一组基，f的度量阵为O<sub>n</sub>.

2. 若$\exists a, b, f(a, b) \neq  0，断言\exists e_{1} \in V,$成立f(e<sub>1</sub>, e<sub>1</sub>) $\ne$  0.否则,$\forall e \in V, f(e, e) = 0,$则由对称性和线性性

4f(a, b) = f(a + b, a + b) -f(a -b, a -b) = 0,

矛盾。取e<sub>1</sub>为V 的一个基向量，并扩充为一组基{e<sub>1</sub>, · · ·, e<sub>n</sub>},令

$$
\varepsilon _1= e_1,
 = e_i -f(e_1, e_i)
 \varepsilon _i f(e_1, e_1)e1, i = 2, \cdots, n.
$$

那么，f($\varepsilon$<sub>1</sub>, $\varepsilon$<sub>i</sub>) = 0, (i = 2, · · ·, n).再考察$\tilde{V}$ = span($\varepsilon$<sub>2</sub>, · · ·, $\varepsilon$<sub>n</sub>),容易验证

$$
V = span(\varepsilon _1) ⊕\tilde V,
$$

而f($\varepsilon$<sub>1</sub>, x) = 0, $\forall x \in ˜V.$由f的对称性，它在$\tilde{V}$ 上也对称。对dim $\tilde{V}$ = n -1,由假设，$\exists${$\tilde{varepsilon}$<sub>2</sub>, · · ·, $\tilde{varepsilon}$<sub>n</sub>} 为$\tilde{V}$ 的一组基，f在$\tilde{V}$ 上的度量阵

$$
(f(\tilde \varepsilon _i, \tilde \varepsilon _j))_(n-1)\times (n-1) = diag(I_\tilde p, -I_ \tilde m, O).
$$

{    }

$$
1
$$

现在以 $\tilde{varepsilon}$<sub>1</sub> =$\sqrt$ e<sub>1</sub>, $\tilde{varepsilon}$<sub>2</sub>, · · ·, $\tilde{varepsilon}$<sub>n</sub>为V 的一组基，此时f 的

$$
|f(e_1, e_1)|
$$

度量阵

$$
(f(\tilde \varepsilon _i, \tilde \varepsilon _j))_n\times n = diag(sign(f(e_1, e_1)), I_\tilde p, -I_ \tilde m, O).
$$

经过基的排列顺序调整，即可使之变为形如diag(I<sub>p</sub>, -I<sub>m</sub>, O) 的对角阵。

归纳证毕。上面的操作其实就是通过线性替换求二次型的规范形。    ■

由定理推知，对称双线性型在适当的基下表示为

$$
f(a, b) = (x^1y^1 + \cdots + x^py^p) -(x^p+1y^p+1 + \cdots + x^ry^r).
$$

如前所述，在取定基以后，双线性型f(a, b) = x$^\mathsf{T}$Ay，若A(双线性型)对称，那么x$^\mathsf{T}$Ax如前所述就是一个二次型。因此，称对称双线性型f(a, b) 诱导出的f(a, a)为二次型。反过来，

$$
= 1
$$

f(a, b) 4[f(a + b, a + b) -f(a -b, a -b)]

$$
= 1
$$

2[f(a + b, a + b) -f(a, a) -f(b, b)],

双线性型也唯一确定了一个对称双线性型。

同样可给出反对称双线性型的一种标准形式。

**定理9.3.** 若f为线性空间V 上的反对称双线性型，则$\exists$r,可选一组基

{$\varepsilon$<sub>1</sub>, $\varepsilon$<sub>-1</sub>, · · ·, $\varepsilon$<sub>r</sub>, $\varepsilon$<sub>-r</sub>, $\eta$<sub>1</sub>, · · ·, $\eta$<sub>n-2r</sub>},

f 在这组基下的度量阵为块对角阵

$$
([ ] [ ])
 01 01
$$

diag   , · · ·,, O<sub>n-2r</sub>.

$$
-1 0 -1 0
$$

证明. 若dim V = 1,此时任选一组基$\eta$<sub>1</sub>，反对称度量阵A = (0)本身就是对角形.

设dim V $\le$n -1时定理结论成立。考虑dim V = n,$\forall$f 为V 上的反对称双线性型。

1. 若$\forall a, b \in V$，成立f(a, b) = 0，则f $\equiv$0,任取一组基，f的度量阵为O<sub>n</sub>.

2. 若$\exists$a, b, f(a, b) $\ne$  0，断言a $\ne$  b.否则由反对称性

$$
f(a, a) = -f(a, a) = 0,
$$

矛盾。b取$\varepsilon$<sub>1</sub> = a, $\varepsilon$<sub>-1</sub> =f(a, b),于是

$$
f(\varepsilon _1, \varepsilon _-1) = -f(\varepsilon _-1, \varepsilon _1) = 1.
$$

将{$\varepsilon$<sub>1</sub>, $\varepsilon$<sub>-1</sub>}扩张为V 的一组基{$\varepsilon$<sub>1</sub>, $\varepsilon$<sub>-1</sub>, e<sub>2</sub>, e<sub>3</sub>, · · ·, e<sub>n-1</sub>}. 再令

$\tilde{varepsilon}$<sub>i</sub> = e<sub>i</sub> -f(e<sub>i</sub>, $\varepsilon$<sub>1</sub>)$\varepsilon$<sub>1</sub> + f(e<sub>i</sub>, $\varepsilon$<sub>-1</sub>)$\varepsilon$<sub>-1</sub>, i = 2, · · ·, n -1.

则直接验算知

$$
f(\tilde \varepsilon _i, \varepsilon _1) = f(\tilde \varepsilon _i, \varepsilon _-1) = 0.
$$

再考察$\tilde{V}$ = span(e<sub>2</sub>, · · ·, e<sub>n-1</sub>),容易验证

$$
V = span(\varepsilon _1, \varepsilon _-1) ⊕\tilde V,
$$

而f($\varepsilon$<sub>1</sub>, x) = f($\varepsilon$<sub>-1</sub>, x) = 0, $\forall x \in ˜V.$由f的反对称性，它在$\tilde{V}$ 上也反对称，由dim $\tilde{V}$ = n -2及归纳假设，$\exists${$\varepsilon$<sub>2</sub>, $\varepsilon$<sub>-2</sub>, · · ·, $\varepsilon$<sub>r</sub>, $\varepsilon$<sub>-r</sub>, $\eta$<sub>1</sub>, · · ·, $\eta$<sub>n-2r</sub>} 为$\tilde{V}$ 的一组基，f 在$\tilde{V}$ 上的度量阵为形如

$$
([ ] [ ])
 01 01
$$

diag   , · · ·,, O<sub>n-2r</sub>

$$
-1 0 -1 0
$$

的块对角阵。现在以{$\varepsilon$<sub>1</sub>, $\varepsilon$<sub>-1</sub>, · · ·, $\varepsilon$<sub>r</sub>, $\varepsilon$<sub>-r</sub>, $\eta$<sub>1</sub>, · · ·, $\eta$<sub>n-2r</sub>} 为V 的一组基，此时f 的度量阵即如定理所述。

归纳证毕。    ■

定理表明，在适当的基下，反对称双线性型表示为

$$
f(a, b) = (x^2y^1 -x^1y^2) + \cdots + (x^2ry^2r-1 -x^2r-1y^2r)
$$

由于双线性型与度量阵的对应，定理也就给出了反对称阵的相合标准形。此外，反对称阵的秩总是偶数；若反对称阵非退化，其阶数必为偶数。

V 上定义了双线性型f，记为(V, f).若f对称，称V 为准欧氏空间；若f反对称，称V 为辛(symplectic)空间。

## 9.3 张量的概念与表述

一言以蔽之，张量就是多线性泛函。

**定义9.5.** V<sub>1</sub>, · · ·, V<sub>p</sub>为有限维实线性空间，若映射

$$
f :V_1 \times \cdots \times V_p\to R
 v_1, \cdots, v_p\mapstof(v_1, \cdots, v_p),
$$

对每一个变量v<sub>i</sub>都保线性，即

f(v<sub>1</sub>, · · ·, v<sub>i-1</sub>, λv<sub>i</sub> + µ$\tilde{v}$<sub>i</sub>, v<sub>i+1</sub>, · · ·, v<sub>p</sub>)

$$
=\lambda f(v_1, \cdots, v_i-1, v_i, v_i+1, \cdots, v_p)
$$

+µf(v<sub>1</sub>, · · ·, v<sub>i-1</sub>, $\tilde{v}$<sub>i</sub>, v<sub>i+1</sub>, · · ·, v<sub>p</sub>),

则称之为p阶张量(tensor).

对于张量，自然定义加法和数乘：

$$
(f + g)(v_1, \cdots, v_p) = f(v_1, \cdots, v_p) + g(v_1, \cdots, v_p),
 (\lambda f)(v_1, \cdots, v_p) = \lambda f(v_1, \cdots, v_p).
$$

这样定义加法和数乘得到的也是张量，且所有这样的张量构成线性空间，记为

$$
L(V_1, \cdots, V_p; \mathbb{R}) = V^{*}_1 ⊗\cdots ⊗V^{*}_p.
$$

**例9.10.** p = 1时，线性泛函是张量的特例；p = 2且V<sub>1</sub> = V<sub>2</sub>时，双线性型是张量的特例。

**例9.11.** V<sub>1</sub>, · · ·, V<sub>p</sub>为实线性空间，f<sup>1</sup> $\in V^{*}$<sub>1</sub>, · · ·, f<sup>p</sup> $\in V^{*}$<sub>p</sub>,按以下方式定义张量积f = f<sup>1</sup> $\otimes$· · · $\otimes$f<sup>p</sup>:

f(v<sub>1</sub>, · · ·, v<sub>p</sub>) = f<sup>1</sup>(v<sub>1</sub>) · · · f<sup>p</sup>(v<sub>p</sub>),$\forall v_{1} \in V_{1}, \cdots, v_{p} \in V_{p}.$

则f是V<sub>1</sub>, · · ·, V<sub>p</sub>上的多线性泛函。

形象地说，p阶张量f就是翻p个牌子的函数：依次翻V<sub>1</sub>, · · ·, V<sub>p</sub>中的各一张向量的牌子，就给出一个得数，要求这个翻牌子的函数关于每一张牌子都保持线性性。

前面知道dim $V^{*}$= dim V,在张量也有相应的结论：

$$
dim(V^{*}_1 ⊗\cdots ⊗V^{*}_p) = dim V_1 \cdots dim V_p.
$$

事实上，若{e<sup>1</sup> <sub>k</sub>, · · ·, e<sup>n</sup>k<sub>k</sub> }为$V^{*}$<sub>k</sub> 的一组基(k = 1, · · ·, p)，<sup>11</sup>那么以下n<sub>1</sub> · · · n<sub>p</sub>  个张量（上一个例子中的特殊情况）

$$
e^j1_1 ⊗\cdots ⊗ejp_p, 1 ≤j_k ≤n_k, 1 ≤k ≤p,
$$

 构成$V^{*}$<sub>1</sub> $\otimes$· · · $\otimes V^{*}$<sub>p</sub> 的一组基。

从表象上说，对于这里的每一个张量，如果给它的牌子是V<sub>1</sub>, · · ·, V<sub>p</sub>中依次各选一个基向量，共有n<sub>1</sub> · · · n<sub>p</sub> 种取法。当且仅当这套牌子恰好是e<sup></sup><sub>j1</sub>, · · ·, e<sup></sup>1 <sub>jp</sub>时取值为1，否则一定得0.由此可见，这些牌子是相互“独立”p的，也就是线性无关的。

严格的证明如下。记

f<sub>j1···jp</sub> = f(e<sup></sup><sub>j1</sub>, · · ·, e<sup></sup>1 <sub>jp</sub>), 1 $\le$j<sub>k</sub> $\le$n<sub>k</sub>, 1 $\le$k $\le$p,p

那么，$\forall$v<sub>1</sub> = v<sup>j</sup>1<sub>1</sub> e<sup></sup><sub>j1</sub>, · · ·, v<sub>p</sub> = v1 jp<sub>p</sub> e<sup></sup><sub>jp</sub>,根据多线性性p      f(v<sub>1</sub>, · · ·, v<sub>p</sub>)= f(v<sup>j</sup>1<sub>1</sub> e<sup></sup><sub>j1</sub>, · · ·, v1 jp<sub>p</sub> e<sup></sup><sub>jp</sub>)p  

$$
= v^j1_1 \cdots vjp_p f(e^⃝_j1, \cdots, e^⃝1 _jp)p
$$

 

$$
= f_j1\cdotsjpv^j1_1 \cdots vjp
$$

 p

$$
= f_j1\cdotsjp(e^j1_1 ⊗\cdots ⊗ejp_p)(v_1,\cdots, v_p).
$$

 于是，

$$
f = f_j1\cdotsjp(e^j1_1 ⊗\cdots ⊗ejp_p),
$$

即必定可用e<sup>j</sup>1 <sub>1</sub> $\otimes$· · · $\otimes$e<sub>p</sub> (1 $\le$j<sub>k</sub> $\le$n<sub>k</sub>, 1 $\le$k $\le$p)线性表出。

> 注11：是Vk的基{e1, · · ·, ek nk} 的对偶基，注意：1，两组基的上下标是翻过来的；2，这里的k是Vk 的k序号，不是张量的指标，不参与求和之类的运算，视记号方便放在上下标位置均可。

这里的f<sub>j1···jp</sub> (1 $\le$j<sub>k</sub> $\le$n<sub>k</sub>, 1 $\le$k $\le$p)称为张量f在这组基下的分量，或者坐标。

最后，设若有一组系数$\lambda$<sub>j1···jp</sub> (1 $\le$j<sub>k</sub> $\le$n<sub>k</sub>, 1 $\le$k $\le$p),成立

$$
\lambda _j1\cdotsjp(e^j1_1 ⊗\cdots ⊗ejp_p) = 0.
$$

 

那么，任取一组指标1 $\le$j<sup>$\ast$</sup> <sub>k</sub> $\le$n<sub>k</sub>, 1 $\le$k $\le$p，计算

$$
0= \lambda _j1\cdotsjp(e^j1_1 ⊗\cdots ⊗ejp_p)(e^⃝1_1, \cdots, e^⃝p_p)
$$

 j$\ast$ j$\ast$

$$
= \lambda _j1\cdotsjpe^j1_1 (e^⃝1_1) \cdots ejp_p (e^⃝p_p)
$$

j$\ast$ j$\ast$

$$
= \lambda _j1\cdotsjp\delta ^j1_1 \cdots \delta jp
$$

j$\ast$ p$\ast$

$$
= \lambda _j∗_p.
 1 \cdotsj∗
$$

由1 $\le$j<sup>$\ast$</sup><sub>k</sub> $\le$n<sub>k</sub>, 1 $\le$k $\le$p的任意性，上述多线性泛函e<sup>j</sup>1    <sub>1</sub> $\otimes$· · · $\otimes$ejp<sub>p</sub> (1 $\le$ j<sub>k</sub> $\le$n<sub>k</sub>, 1 $\le$k $\le$p)线性无关。

综上，证毕。

用一个具体的例子来予以说明。考虑U = $\mathbb{R}^2$, V = $\mathbb{R}^3$,记U中向量u = (u<sup>1</sup>, u<sup>2</sup>),V 中向量v = (v<sup>1</sup>, v<sup>2</sup>, v<sup>3</sup>).因为有内积结构，U, V 分别以

$$
e_1 = (1, 0), e_2 = (0, 1)
$$

及

$$
\varepsilon _1 = (1, 0, 0), \varepsilon _2 = (0, 1, 0), \varepsilon _3 = (0, 0, 1)
$$

为基，而里兹表示定理指出$U^{*}$, $V^{*}$中线性泛函的作用就表示为取内积，而对偶基同样为

$$
e^1 = (1, 0), e^2 = (0, 1),
$$

及

$$
\varepsilon ^1 = (1, 0, 0), \varepsilon ^2 = (0, 1, 0), \varepsilon ^3 = (0, 0, 1).
$$

这样的定义下，

$$
e^1(u)= (u, e^1)= u^1,
 e^2(u)= (u, e^2)= u^2;
 \varepsilon ^1(v)= (v, \varepsilon ^1)= v^1,
 \varepsilon ^2(v)= (v, \varepsilon ^2)= v^2;
 \varepsilon ^3(v)= (v, \varepsilon ^3)= v^3.
$$

再看

$$
(e^1 ⊗\varepsilon ^1)(u, v)= e^1(u)\varepsilon ^1(v)= u^1v^1,
 (e^1 ⊗\varepsilon ^2)(u, v)= e^1(u)\varepsilon ^2(v)= u^1v^2,
 (e^1 ⊗\varepsilon ^3)(u, v)= e^1(u)\varepsilon ^3(v)= u^1v^3,
 (e^2 ⊗\varepsilon ^1)(u, v)= e^2(u)\varepsilon ^1(v)= u^2v^1,
 (e^2 ⊗\varepsilon ^2)(u, v)= e^2(u)\varepsilon ^2(v)= u^2v^2,
 (e^2 ⊗\varepsilon ^3)(u, v)= e^2(u)\varepsilon ^3(v)= u^2v^3.
$$

U, V 上的双线性泛函f,首先由于关于u $\in$U的线性性，必表示为

$$
f(u, v) = \varphi (v)u^1 + \psi (v)u^2,
$$

特别的，

$$
f(e_1, v) = \varphi (v)
$$

关于v $\in$V 线性就必有

$$
\varphi (v) = f_11v^1 + f_12v^2 + f_13v^3.
$$

同理，

$$
\psi (v) = f_21v^1 + f_22v^2 + f_23v^3.
$$

于是


[    ]v<sup>1</sup>

$$
f_11f_12f_13
$$

f(u, v)= (u<sup>1</sup>, u<sup>2</sup>)    v<sup>2</sup>

$$
f_21f_22f_23
$$

v<sup>3</sup> = f<sub>11</sub>u<sup>1</sup>v<sup>1</sup> + f<sub>12</sub>u<sup>1</sup>v<sup>2</sup> + f<sub>13</sub>u<sup>1</sup>v<sup>3</sup> + f<sub>21</sub>u<sup>2</sup>v<sup>1</sup> + f<sub>22</sub>u<sup>2</sup>v<sup>2</sup> + f<sub>23</sub>u<sup>2</sup>v<sup>3</sup>.

故f正是上面6个基向量e<sup>1</sup> $\otimes$$\varepsilon$<sup>1</sup>, e<sup>1</sup> $\otimes$$\varepsilon$<sup>2</sup>, e<sup>1</sup> $\otimes$$\varepsilon$<sup>3</sup>, e<sup>2</sup> $\otimes$$\varepsilon$<sup>1</sup>, e<sup>2</sup> $\otimes$$\varepsilon$<sup>2</sup>, e<sup>2</sup> $\otimes$$\varepsilon$<sup>3</sup> 的线性组合。

从这个例子容易看到，2阶张量可以用一个矩阵（矩阵的阶由两个空间U, V 的维数给出）来表示。这就是通常说“1阶张量为向量，2 阶张量为矩阵”的来历。

对没有内积结构的U, V 可完全类似地解读，仅改变记号以示区别。不妨仍设dim U = 2, dim V = 3,若U的基为$\eta$<sub>1</sub>, $\eta$<sub>2</sub>,V 的基为$\zeta$<sub>1</sub>, $\zeta$<sub>2</sub>, $\zeta$<sub>3</sub>，其中各一任意元素

u = u<sup>1</sup>$\eta$<sub>1</sub> + u<sup>2</sup>$\eta$<sub>2</sub>,v = v<sup>1</sup>$\zeta$<sub>1</sub> + v<sup>2</sup>$\zeta$<sub>2</sub> + v<sup>3</sup>$\zeta$<sub>3</sub>.

任一张量f,由关于u线性知

$$
f(u, v) = f_1(v)u^1 + f_2(v)u^2.
$$

考虑特例u<sup>1</sup> = 1, u<sup>2</sup> = 0,再由关于v线性知

$$
f_1(v) = f_11v^1 + f_12v^2 + f_13v^3,
$$

同理

$$
f_2(v) = f_21v^1 + f_22v^2 + f_23v^3,
$$

于是同样有

$$
= f_11u^1v^1 + f_12u^1v^2 + f_13u^1v^3 + f_21u^2v^1 + f_22u^2v^2 + f_23u^2v^3
$$

f(u, v)


[    ]v<sup>1</sup>

$$
f_11f_12f_13.
 = (u^1, u^2) v^2
 f_21f_22f_23
$$

v<sup>3</sup>

通过前面奇异值分解知道，系数矩阵(f<sub>ij</sub>)<sub>2$\times$3</sub>可以分解成至多两个秩1矩阵之和。按照张量基分解是一种更为“浪费”的方案：[    ]

$$
f_11f_12f_13
 f_21f_22f_23
 [] [] []
$$

1    1    1

$$
=f_11 [1, 0, 0] + f_12[0, 1, 0] + f_13[0, 0, 1]
$$

0    0    0

$$
[] [] []
$$

0    0    0 +f<sub>21</sub> [1, 0, 0] + f<sub>22</sub>[0, 1, 0] + f<sub>23</sub>[0, 0, 1]. 1    1    1

而这里的每一项中，如

$$
[]
 1
 [1, 0, 0]
 0
$$

给出了

$$
([ ]) v^1 ( []) v^1
 1 = 1
$$

[u<sup>1</sup>, u<sup>2</sup>] [1, 0, 0]v<sup>2</sup> [u<sup>1</sup>, u<sup>2</sup>] [1, 0, 0]v<sup>2</sup>= u<sup>1</sup>v<sup>1</sup>, 0    0 v<sup>3</sup>    v<sup>3</sup>

就恰好是两个线性泛函的张量积$\tilde{eta}$<sub>1</sub> $\otimes$$\tilde{zeta}$<sub>1</sub>，各自给出

$$
\tilde \eta ^1(u) = u^1,\tilde \zeta ^1(v) = v^1.
$$

于是，$U^{*}$$\otimes V^{*}$就由这样的6个张量积表示出来的基张出来。

回到一般的情形。如果给定一组线性空间V<sub>1</sub>, · · ·, V<sub>p</sub>,它们各自的对偶空间$V^{*}$<sub>1</sub>, · · ·, $V^{*}$<sub>p</sub> 也是线性空间，且不改变维数；那么从这些对偶空间出发，

自然可以定义它们的张量积空间V<sup>$\ast$$\ast$</sup>    $\otimes$· · · $\otimes$V<sup>$\ast$$\ast$</sup><sub>p</sub>,并且如果采用自然同构，

$$
1
$$

就是V<sub>1</sub> $\otimes$· · · $\otimes$V<sub>p</sub>.同样有

$$
dim(V_1 ⊗\cdots ⊗V_p) = dim V_1 \cdots dim V_p.
$$

并且，若(e<sup></sup> <sub>1</sub>, · · ·, e<sup></sup>k <sub>nk</sub>)为V<sub>k</sub>的一组基(k = 1, · · ·, p)，那么n<sub>1</sub> · · · n<sub>p</sub>个以下多k线性泛函

$$
e^⃝_j1 ⊗\cdots ⊗e^⃝1 _jp, 1 ≤j_k ≤n_k, 1 ≤k ≤p,p
$$

构成V<sub>1</sub> $\otimes$· · · $\otimes$V<sub>p</sub>的一组基。

值得注意的是，从上面的定义和例子可以看出，V<sub>1</sub> $\otimes$· · · $\otimes$V<sub>p</sub>并不只包含形如v<sub>1</sub> $\otimes$· · · $\otimes$v<sub>p</sub> 的元素（容易验证v<sub>1</sub> $\otimes$· · · $\otimes$v<sub>p</sub> $\in$V<sub>1</sub> $\otimes$· · · $\otimes$V<sub>p</sub>,即为($V^{*}$<sub>1</sub>, · · ·, $V^{*}$<sub>p</sub> )上的张量）。例如$\mathbb{R}^2 \otimes \mathbb{R}^2$ 包含所有二阶方阵，而v<sub>1</sub> $\otimes$v<sub>2</sub> 仅能表示秩1的二阶方阵和零方阵。

然而下述二者却是可以等同的。以上面的二阶例子来说：dim U = 2, dim V = 3.这里

$$
f \in U^{*}⊗V^{*}= L(U, V ; \mathbb{R}),
$$

而

$$
T \in (U ⊗V)^{*}= (L(U^{*}, V^{*}; \mathbb{R}))^{*}.
$$

$\forall u \in U, $v \in V$,$若u, v坐标分别为(u<sup>1</sup>, u<sup>2</sup>), (v<sup>1</sup>, v<sup>2</sup>, v<sup>3</sup>),f有矩阵表示：

f(u, v)


[    ]v<sup>1</sup>

$$
f_11f_12f_13
$$

=[u<sup>1</sup>, u<sup>2</sup>]    v<sup>2</sup>

$$
f_21f_22f_23
$$

v<sup>3</sup>

f<sub>11</sub>u<sup>1</sup>v<sup>1</sup> + f<sub>12</sub>u<sup>1</sup>v<sup>2</sup> + f<sub>13</sub>u<sup>1</sup>v<sup>3</sup> + f<sub>21</sub>u<sup>2</sup>v<sup>1</sup> + f<sub>22</sub>u<sup>2</sup>v<sup>2</sup> + f<sub>23</sub>u<sup>2</sup>v<sup>3</sup>, =

于是f由这2 $\times$ 3的矩阵唯一确定。

而在上面取的基$\eta$<sub>i</sub> $\otimes$$\zeta$<sub>j</sub>下，

u $\otimes$v = u<sup>1</sup>v<sup>1</sup>$\eta$<sub>1</sub> $\otimes$$\zeta$<sub>1</sub> + u<sup>1</sup>v<sup>2</sup>$\eta$<sub>1</sub> $\otimes$$\zeta$<sub>2</sub> + u<sup>1</sup>v<sup>3</sup>$\eta$<sub>1</sub> $\otimes$$\zeta$<sub>3</sub>

$$
+u^2v^1\eta _2 ⊗\zeta _1 + u^2v^2\eta _2 ⊗\zeta _2 + u^2v^3\eta _2 ⊗\zeta _3,
$$

于是T 也就由六个系数

T $\eta$<sub>1</sub> $\otimes$$\zeta$<sub>1</sub>, T $\eta$<sub>1</sub> $\otimes$$\zeta$<sub>2</sub>, T $\eta$<sub>1</sub> $\otimes$$\zeta$<sub>3</sub>, T $\eta$<sub>2</sub> $\otimes$$\zeta$<sub>1</sub>, T $\eta$<sub>2</sub> $\otimes$$\zeta$<sub>2</sub>, T $\eta$<sub>2</sub> $\otimes$$\zeta$<sub>3</sub>

唯一确定。

只要指定

$$
T (\eta _i ⊗\zeta _j) = f(\eta _i, \zeta _j),
$$

把这两者的6个系数一一对等起来，就建立了f, T 之间的1-1映射。以后对二者不加区分。

选读内容:

一般情形下的定理及证明如下。

**定理9.4.** $\forall f \in V^{*}$<sub>1</sub> $\otimes$· · · $\otimes V^{*}$<sub>p</sub>, $\exists$唯一张量T $\in$(V<sub>1</sub> $\otimes$· · · $\otimes$V<sub>p</sub>)<sup>$\ast$</sup>,成立

$$
f(v_1, \cdots, v_p) = T (v_1 ⊗\cdots ⊗v_p), \forall v_i \in V_i, 1 ≤i ≤p.
$$

证明. 取(e<sup></sup><sub>j1</sub> $\otimes$· · · $\otimes$e<sup></sup>1 <sub>jp</sub>) (1 $\le$j<sub>i</sub> $\le$n<sub>i</sub>, 1 $\le$i $\le$p)为V<sub>1</sub> $\otimes$· · · $\otimes$V<sub>p</sub> 的一组基。p定义

$$
T e^⃝_j1 ⊗\cdots ⊗e^⃝1 _jp = f(e^⃝p _j1, \cdots, e^⃝1 _jp) (1 ≤j_i ≤n_i, 1 ≤i ≤p),p
$$

这就唯一确定了V<sub>1</sub> $\otimes$· · · $\otimes$V<sub>p</sub>上的线性泛函T，即$\forall$v<sub>1</sub> = v<sup>j</sup>1    <sub>1</sub> e<sup></sup><sub>j1</sub>, · · ·, v<sub>p</sub> =1  vjp<sub>p</sub> e<sup></sup><sub>jp</sub>,p 

$$
T (v_1 ⊗\cdots ⊗v_p)= T (v^j1_1 e^⃝_j1 ⊗\cdots ⊗v1 jp_p e^⃝_jp)p
$$

 

$$
= v^⃝_j1 \cdots v^⃝1 _jpT (e^⃝p _j1 ⊗\cdots ⊗e^⃝1 _jp).p
$$

断言张量f也同样由这n<sub>1</sub> · · · n<sub>p</sub>个数f(e<sup></sup> <sub>j1</sub>, · · ·, e<sup></sup>1 <sub>jp</sub>) (1 $\le$j<sub>i</sub> $\le$n<sub>i</sub>, 1 $\le$i $\le$p p) 唯一确定。事实上，跟前面一样，由多线性性

$$
f(v_1, \cdots, v_p)= f(v^j1_1 e^⃝_j1, \cdots, v1 jp_p e^⃝_jp)p
$$

 

$$
= v^⃝_j1 \cdots v^⃝1 _jpf(e^⃝p_j1, \cdots, e^⃝1 _jp).p
$$

综上证毕。    ■

上面确定张量T 的n<sub>1</sub> · · · n<sub>p</sub>个数

$$
T e^⃝_j1 ⊗\cdots ⊗e^⃝1 p
$$

jp称为它在前述基（或者说{e<sup></sup> <sub>1</sub>, · · ·, e<sup></sup>1 <sub>n1</sub>; · · · ; e<sup></sup>1 <sub>1</sub>, · · ·, e<sup></sup>p <sub>np</sub>}）下的坐标。p

定理的证明过程给出了同构(容易验证保持线性)

$$
V^{*}_1 ⊗\cdots ⊗V^{*}_p \cong (V_1 ⊗\cdots ⊗V_p)^{*}.
$$

而利用V<sup>$\ast$$\ast$</sup>$\sim$= V 知道映射

$$
(v_1, \cdots, v_p) \mapstov_1 ⊗\cdots ⊗v_p
$$

是线性的。

更一般地，可考虑张量f的变量分别在V<sub>1</sub>, · · ·, V<sub>r</sub>, $V^{*}$ <sub>r+1</sub>, · · ·, $V^{*}$<sub>p</sub> 中，即p张牌子中前r个是向量，后p -r 个是泛函。

**定义9.6.** f $\in V^{*}$<sub>1</sub> $\otimes$· · $\cdot V^{*}$<sub>r</sub> $\otimes$V<sub>r+1</sub> $\otimes$· · · $\otimes$V<sub>p</sub>称为关于V<sub>1</sub>, · · ·, V<sub>r</sub>协变、关于V<sub>r+1</sub>, · · ·, V<sub>p</sub> 逆变的(r, p-r)型张量。特别的，f $\in V^{*}$ <sub>1</sub> $\otimes$· · ·$\otimes V^{*}$<sub>p</sub> 为p 阶协变（covariant）张量；f $\in$V<sub>1</sub> $\otimes$· · · $\otimes$V<sub>p</sub>为p阶逆变（contravariant）张量。

逆变和协变的叫法可以通过坐标如何跟着基变化来解释。以一阶张量为例，如果取V 的基{e<sub>1</sub>, · · ·, e<sub>n</sub>}时f(在对偶基{e<sup>1</sup>, · · ·, e<sup>n</sup>} 下)的坐标表示为

$$
f = f_1e^1 + \cdots + f_ne^n,
$$

那么从对偶基的定义直接计算可得

$$
f(e_i) = f_i.
$$

将V 的基伸长，譬如变为{2e<sub>1</sub>, e<sub>2</sub>, · · ·, e<sub>n</sub>},则新的对偶基{<sup>1</sup> <sub>2</sub>e<sup>1</sup>, · · ·, e<sup>n</sup>}下有

$$
(1)
 f = 2f_12e^1+ \cdots + f_ne^n,
$$

这里

$$
f(2e_1) = 2f_1,
$$

即坐标相应地伸长（协变）。

另一方面，对于V 中原来基向量下坐标表示为v = v<sub>1</sub>e<sub>1</sub> + · · · + v<sub>n</sub>e<sub>n</sub>的向量，在伸长了的新的基下显然是

$$
v = 1
$$

2v<sub>1</sub>(2e<sub>1</sub>) + v<sub>2</sub>e<sub>2</sub> + · · · + v<sub>p</sub>e<sub>p</sub>,

即坐标反而缩短（逆变）。

当然，从代数的角度看，协变、逆变只是看哪一个线性空间为观察的出发点。但在物理或力学应用中，常常考虑部分V<sub>i</sub>同构于（或者就是）$\mathbb{R}^{3}$，其它同构于其对偶空间，这时选定$\mathbb{R}^{3}$的一组基后，就有些是协变、另一些逆变了。

选读内容:

在数学上，协变、逆变也有直接的应用。

**定理9.5.** $U^{*} \otimes$V $\sim$= $L(U, V )$.

仍以前面的dim U = 2, dim V = 3为例解释。若f $\in U^{*} \otimes$V 为关于(U, V )的(1,1)张量。$\forall u = u^{1}η_{1}+u^{2}η_{2} \in U, g = g_{1}ζ^{1}+g_{2}ζ^{2}+g_{3}ζ^{3} \in V^{*}$,由于f为线性泛函，一定可表示为

$$
f(u, g) = t^1_1u^1g_1 + t^2_1u^1g_2 + t^3_1u^1g_3 + t^1_2u^2g_1 + t^2_2u^2g_2 + t^3_2u^2g_3,
$$

整理可知

$$
f(u, g) = g_1(t^1_1u^1 + t^2_1u^2) + g_2(t^2_1u^1 + t^2_2u^2) + g_3(t^3_1u^1 + t^3_2u^2).
$$

定义线性变换

$$
T u = (t^1_1u^1 + t^1_2u^2)\zeta _1 + (t^2_1u^1 + t^2_2u^2)\zeta _2 + (t^3_1u^1 + t^3_2u^2)\zeta _3,
$$

那么，在前述基下其矩阵表示就是

$$
[ ]
$$

t<sup>1</sup>t<sup>2</sup>t<sup>3</sup>

$$
111,
$$

t<sup>1</sup>t<sup>2</sup>t<sup>3</sup>

$$
222
$$

且计算可知

g(T u)

(g<sub>1</sub>$\zeta$<sup>1</sup> + g<sub>2</sub>$\zeta$<sup>2</sup> + g<sub>3</sub>$\zeta$<sup>3</sup>)[(t<sup>1</sup><sub>1</sub>u<sup>1</sup> + t<sup>1</sup><sub>2</sub>u<sup>2</sup>)$\zeta$<sub>1</sub> + (t<sup>2</sup><sub>1</sub>u<sup>1</sup> + t<sup>2</sup><sub>2</sub>u<sup>2</sup>)$\zeta$<sub>2</sub> + (t<sup>3</sup><sub>1</sub>u<sup>1</sup> + t<sup>3</sup><sub>2</sub>u<sup>2</sup>)$\zeta$<sub>3</sub>] =

g<sub>1</sub>(t<sup>1</sup><sub>1</sub>u<sup>1</sup> + t<sup>1</sup><sub>2</sub>u<sup>2</sup>) + g<sub>2</sub>(t<sup>2</sup><sub>1</sub>u<sup>1</sup> + t<sup>2</sup><sub>2</sub>u<sup>2</sup>) + g<sub>3</sub>(t<sup>3</sup><sub>1</sub>u<sup>1</sup> + t<sup>3</sup><sub>2</sub>u<sup>2</sup>) = =f(u, g).

T 和f均由6个系数t<sup>1</sup> <sub>1</sub>, t<sup>2</sup><sub>1</sub>, t<sup>3</sup><sub>1</sub>, t<sup>1</sup><sub>2</sub>, t<sup>2</sup><sub>2</sub>, t<sup>3</sup><sub>2</sub>唯一确定，于是相互之间可以构造1- 1映射；该映射保持线性，故为同构。

上述结论可以进一步推广为：(p, 1)阶张量f $\in V^{*}$    <sub>1</sub> $\otimes$· · · $\otimes V^{*}$<sub>p</sub> $\otimes$V 唯一决定一个V<sub>1</sub> $\otimes$· · · $\otimes$V<sub>p</sub> $\to$ V 的线性映射。

**定理9.6.** $V^{*}$<sub>1</sub> $\otimes$· · · $\otimes V^{*}$<sub>p</sub> $\otimes$V $\sim$= $L(V_{1} \otimes \cdots \otimes V_{p}, V)$.

这里的线性映射不再表示为一个矩阵，而是用下标的方式记为(t<sup>j</sup>    <sub>j1···jp</sub>),而

$$
v^j = T (v_1, \cdots, v_p)^j = t^j_j1\cdotsjpv^j1jp
$$

<sub>1</sub> · · · v<sub>p</sub>,  这里j = 1, · · ·, dim V.

记号对于代数表达和计算是基本工具，总结一下前面用到的爱因斯坦记号(Einstein convention).包括两部分：

1. V 中的元（向量）坐标用上标（逆变坐标），$V^{*}$中的元（余向量，即看作泛函）坐标用下标（协变坐标）；

2. 关于在表达式中上下标各出现一次的指标求和，其余指标不改变上下位置。

依照这样的规则，线性变换T $\in$$L(U, V )$的矩阵表示记为(t<sup>i</sup>    <sub>j</sub>)是合适的，这时有向量的映射

$$
v^i = t^i_ju^j.
$$

以下利用这样的记号研究基变换后张量坐标的改变规则。为此，先重新讨论向量空间V 中的变换规则，并与之前已知的结论比较以理解上述记号规则。

以3维空间为例，基从{e<sub>1</sub>, e<sub>2</sub>, e<sub>3</sub>}变为<sup>12</sup>

$$
(\tilde e_1, \tilde e_2, \tilde e_3) = (e_1, e_2, e_3)T,
$$

这里T = (t<sup>i</sup><sub>j</sub>)<sub>3$\times$3</sub>,如前所述，上标i为行标，下标j为列标。基变换按分量记为

$$
\tilde e_ℓ= t^i_ℓe_i.
$$

注意一下上下相同表示求和。求和的哑标i在e<sub>i</sub>中是下标，在t<sup>i</sup>    <sub>$\ell$</sub>中是上标。再比较上面的基变换可见，“右上左下”，矩阵表示时，这里矩阵在右。

记任一向量v的坐标分别为


x<sup>1</sup> $\tilde{x}$<sup>1</sup>

$$
及.
$$

T<sup>-1</sup>x<sup>2</sup> $\tilde{x}$<sup>2</sup> x<sup>3</sup> $\tilde{x}$<sup>3</sup>

以下写出张量分量的记号，右栏给出相应的矩阵向量表达式（“上行下列，右上左下”分别给出行列和乘法的序）。

记逆矩阵为

$$
T^{-1} = T = (\tau ^{\mathsf{T}}i
 _j)_3\times 3.
$$

这就是说，$\tau$<sup>i</sup><sub>$\ell$</sub>t<sup>$\ell$</sup><sub>j</sub> = t<sup>i</sup><sub>$\ell$</sub>$\tau$<sup>$\ell$</sup><sub>j</sub> = $\delta$<sup>i</sup><sub>j</sub>, TT = T T = I.

根据向量不变，       $\tilde{x}$<sup>1</sup>    x<sup>1</sup>       

$\tilde{x}$<sup>i</sup>$\tilde{e}$<sub>i</sub> = x<sup>j</sup>e<sub>j</sub>, ($\tilde{e}$<sub>1</sub>, $\tilde{e}$<sub>2</sub>, $\tilde{e}$<sub>3</sub>)$\tilde{x}$<sup>2</sup>= (e<sub>1</sub>, e<sub>2</sub>, e<sub>3</sub>)x<sup>2</sup>.      $\tilde{x}$<sup>3</sup>    x<sup>3</sup>

> 注12：下面分量的记号中并不涉及维数，可见这种记号的一般性。m mi

代入基变换得       $\tilde{x}$<sup>1</sup>    x<sup>1</sup>        $\tilde{x}$<sup>i</sup>t<sup>$\ell$</sup><sub>i</sub>e<sub>$\ell$</sub>= x<sup>j</sup>e<sub>j</sub>, (e<sub>1</sub>, e<sub>2</sub>, e<sub>3</sub>)T$\tilde{x}$<sup>2</sup>= (e<sub>1</sub>, e<sub>2</sub>, e<sub>3</sub>)x<sup>2</sup>.      $\tilde{x}$<sup>3</sup>    x<sup>3</sup>

作用对偶基ek得左右两式分别为e<sup>k</sup>$\tilde{x}$<sup>i</sup>t<sup>$\ell$</sup>= $\tilde{x}$<sup>i</sup>t<sup>$\ell$</sup><sub>i</sub>$\delta$<sup>k</sup>      <sub>i</sub>e<sub>$\ell$</sub> $\ell$    $\tilde{x}$<sup>1</sup>

$$
= \tilde x^it^k_i, 
 取第k个坐标(t^k _1, t^k_2, t^k_3)\tilde x^2= x^k.
$$

 e<sup>k</sup>x<sup>j</sup>e<sub>j</sub> = x<sup>j</sup>$\delta$<sup>k</sup> j    $\tilde{x}$<sup>3</sup>

$$
= x^k,
$$

于是

$$
\tilde x^it^k_i = x^k, 列式即T \tilde x = x.
$$

两边作用$\tau$<sup>s</sup><sub>k</sub>得13

$$
\tilde x^k = x^k\tau ^{\mathsf{T}}s
 _k, \tilde x = Tx.
$$

可以看到，分量的推导中只要用好爱因斯坦约定，不需考虑各项在表达式中的顺序，这比向量、矩阵的描述方式要自由不少。

接着再看对偶基的变换。由定义并利用基变换表达式知

$$
\delta ^i= \tilde e^i\tilde e_j
$$

j

$$
= \tilde e^it^ℓ_je_ℓ,
$$

取i = j得

$$
1 = \tilde e^it^ℓ_ie_ℓ
$$

作用e<sup>m</sup>得（在矩阵向量表示中，由于e<sup>m</sup>, $\tilde{e}$<sup>k</sup>以上标表示，它们本身类似于行向量，放在一起就要按照行堆叠起来）    

$$
e^m = \tilde e^it^ℓ_ie_ℓe^m e^1 \tilde e^1

= \tilde e^it^ℓ_i\delta ^m e^2= T \tilde e^2.
$$

$\ell$     

$$
= \tilde e^it^m_i, e^3 \tilde e^3
$$

再作用$\tau$<sup>k</sup><sub>m</sub>得   

$$
e^m\tau ^{\mathsf{T}}k= \tilde e^it^m_i \tau ^ k \tilde e^1 e^1

= \tilde e^i\delta ^k \tilde e^2= Te^2.

= \tilde e^k, \tilde e^3 e^3
$$

> 注13：因为与式子中上标k相同，实际上是对每一个k作用，之后关于k求和。

矩阵向量表示如果写成行，就是(e<sup>1</sup>, e<sup>2</sup>, e<sup>3</sup>) = ($\tilde{e}$<sup>1</sup>, $\tilde{e}$<sup>2</sup>, $\tilde{e}$<sup>3</sup>)T$^\mathsf{T}$,($\tilde{e}$<sup>1</sup>, $\tilde{e}$<sup>2</sup>, $\tilde{e}$<sup>3</sup>) = (e<sup>1</sup>, e<sup>2</sup>, e<sup>3</sup>)T<sup>-T</sup>.

有意思的是，在爱因斯坦约定下，只要知道了用的是逆矩阵T,上下标只能这么写才能正确地去留。利用这一点，由于泛函f 在对偶基下的变换跟对偶基的变换又差一个求逆，于是泛函的坐标变换表达式要通过T 的分量联系坐标$\tilde{y}$<sub>i</sub>与y<sub>j</sub>, 按照这样的规则只能是

$$
\tilde y_i = t^j_iy_j,
$$

这里的求和是关于t的上标的，再考虑到坐标用的下标表示一行中的各列，在向量、矩阵的表示下就是

$$
(\tilde y_1, \tilde y_2, \tilde y_3) = (y_1, y_2, y_3)T,
$$

亦即以列向量表示坐标时的

$$
\tilde y = T^{\mathsf{T}}y,
$$

这与之前的结论一致。

采用这样的记号系统，讨论基变换后张量的坐标如何改变。

考虑一个(2, 1)型张量f $\in U^{*}$$\otimes V^{*} \otimes$W,若U, V, W基依次取为

{$\xi$<sub>1</sub>, · · ·, $\xi$<sub>p</sub>}; {$\eta$<sub>1</sub>, · · ·, $\eta$<sub>q</sub>}; {$\zeta$<sub>1</sub>, · · ·, $\zeta$<sub>r</sub>},

那么，f(u, v, g)取值为(u = x<sup>i</sup>$\xi$<sub>i</sub>, v = y<sup>j</sup>$\eta$<sub>j</sub>, g = z<sub>k</sub>$\zeta$<sup>k</sup>)

$$
f(u, v, g) = f^{\mathsf{T}}k_ijx^iy^jz_k,
$$

也就是说，张量在形为$\xi$<sup>i</sup> $\otimes$$\eta$<sup>j</sup> $\otimes$$\zeta$<sub>k</sub>的张量组成的基下可表示为

$$
f = f^{\mathsf{T}}k_ij\xi ^i ⊗\eta ^j ⊗\zeta _k.
$$

若U, V, W的基变换为

$$
(\tilde \xi _1, \cdots, \tilde \xi _p)= (\xi _1, \cdots, \xi _p)A;
 (\tilde \eta _1, \cdots, \tilde \eta _q)= (\eta _1, \cdots, \eta _q)B;
 (\tilde \zeta _1, \cdots, \tilde \zeta _r)= (\zeta _1, \cdots, \zeta _r)C,
$$

分量形式写就是

$$
\tilde \xi _i = a^ℓ_i\xi _ℓ,\tilde \eta _j = b^m_j \eta _m,\tilde \zeta _k = c^n_k\zeta _n,
$$

又逆矩阵分别以分量形式记为$\alpha$<sup>i</sup>    <sub>$\ell$</sub>, $\beta$<sup>j</sup><sub>m</sub>, $\gamma$<sup>k</sup><sub>n</sub>,新的基由以下张量组成(i = 1, · · ·, p; j = 1, · · ·, q; k = 1, · · ·, r)

$$
\tilde \xi ^i ⊗\tilde \eta ^j ⊗\tilde \zeta _k = \xi ^ℓ⊗\eta ^m ⊗\zeta _n\alpha ^i_ℓ\beta ^j_mc^n
$$

<sub>k</sub>,

而张量坐标为$\tilde{f}$k<sub>$\tilde{i}$$\tilde{j}$</sub>a<sup>$\tilde{i}$</sup>$\tilde{k}$$\tilde{j}$<sub>j</sub>$\gamma$<sup>k</sup>

$$
_ij = f_ib_\tilde k,
$$

容易验证

$$
\tilde f_ k_ij \tilde \xi ^i ⊗\tilde \eta ^j ⊗\tilde \zeta _k
 _\tilde i\tilde j\xi ^ℓ⊗\eta ^m ⊗\zeta _na^\tilde i\tilde k \tilde j_j\gamma ^k_\tilde k\alpha ^i_ℓ\beta ^j_mc^n
 =f _ib
$$

k

$$
_\tilde i\tilde j\xi ^ℓ⊗\eta ^m ⊗\zeta _n\delta ^\tilde i\tilde k _ℓ\delta ^\tilde j_m\delta ^n
 =f
$$

$\tilde{k}$

$$
f^{\mathsf{T}}n_ℓm\xi ^ℓ⊗\eta ^m ⊗\zeta _n,
 =
$$

即张量f本身是不变的。

特别地，从一个公共的线性空间V 出发，V 的一组基取为{e<sub>1</sub>, · · ·, e<sub>n</sub>},对偶基{e<sup>1</sup>, · · ·, e<sup>n</sup>}.考虑(2, 1)-型张量f $\in V^{*}$$\otimes V^{*} \otimes$V. 对于u = x<sup>i</sup>e<sub>i</sub>, v = y<sup>j</sup>e<sub>j</sub> $\in$V, g = z<sub>k</sub>e<sup>k</sup> $\in V^{*}$,有

$$
f(u, v, g) = f^{\mathsf{T}}k_ijx^iy^jz_k,
$$

以及

$$
f = f^{\mathsf{T}}k_ije^i ⊗e^j ⊗e_k.
$$

V 中坐标变换

$$
(\tilde e_1, \cdots, \tilde e_n) = (e_1, \cdots, e_n)T,
$$

即

$$
\tilde e_i = e_ℓt^ℓ
$$

i引起u, v的坐标变换(t<sup>$\ell$</sup> <sub>i</sub>$\tau$<sup>j</sup><sub>$\ell$</sub>= $\delta$<sup>j</sup><sub>i</sub> )

$$
\tilde x^i = x^ℓ\tau ^{\mathsf{T}}i\tilde y^j = y^m\tau ^{\mathsf{T}}j
 _ℓ, _m,
$$

以及对偶基的变换

$$
\tilde e^k = e^n\tau ^{\mathsf{T}}k
$$

n和g的坐标变换

$$
\tilde z_k = z_nt^n_k.
$$

张量基变换为

$$
\tilde e^i ⊗\tilde e^j ⊗\tilde e_k = e^\tilde i ⊗e^\tilde j ⊗e_\tilde k\tau ^ i_\tilde i \tau ^ j\tilde k
$$

<sub>$\tilde{j}$</sub> t<sub>k</sub>,

而f的坐标也变为

$$
\tilde f_ k_ij = f^ n_ℓmt^ℓ_it^m_j \tau ^ k_n.
$$

在上述变换下，向量与泛函u, v, g不变，张量f不变，取值f(u, v, g)于是也不变。而所有与上标相关的量（泛函坐标、向量的基）用正向的变换(t)，而与下标相关的量（向量坐标、对偶基）用反向的变换($\tau$).

## 9.4 张量运算

本节中讨论从一个线性空间V 出发的张量空间，并用一些例子来作说明。所有讨论容易推广到一般情形。

首先讨论V 是内积空间时的向量和余向量。物理应用中，常常考虑的就是$\mathbb{R}^3$(或$\mathbb{R}^2$)这个内积空间。

举例来说（前面就是这么记的），如果空间V 中设基取为{e<sub>1</sub>, · · ·, e<sub>n</sub>}.向量a = a<sup>1</sup>e<sub>1</sub> + · · · + a<sup>n</sup>e<sub>n</sub>, b = b<sup>1</sup>e<sub>1</sub> + · · · + b<sup>n</sup>e<sub>n</sub>,

之前习惯的内积表达式a<sup>i</sup>b<sup>i</sup>

在爱因斯坦约定下不是一个恰当的表达式，也不能求和。需考虑度量阵

$$
G = (g_ij)_n\times n,
$$

其中g<sub>ij</sub> = (e<sub>i</sub>, e<sub>j</sub>),此时

$$
\sum_{i,j=1}^{n} g_{ij} a^i b^j \equiv g_{ij} a^i b^j
$$

给出了内积(a, b)恰当的表述。<sup>14</sup>

另一方面，由里兹表示定理，内积空间中向量也可以看作余向量，也就是泛函。注意到对偶基{e<sup>1</sup>, · · ·, e<sup>n</sup>}与基的关系，若a 作为余向量的坐标表示为

$$
a = a_1e^1 + \cdots + a_ne^n,
$$

那么

$$
\begin{aligned}
(a, b) &= \sum_{i,j=1}^{n} a_i b^j (e^i, e_j) \\
 &= \sum_{i,j=1}^{n} a_i b^j \delta^i_j \\
 &= \sum_{i=1}^{n} a_i b^i.
\end{aligned}
$$

因此,内积通过向量与余向量的坐标表示，总可以用爱因斯坦约定表示为

$$
(a, b) = a_ib^i = a^ib_i.
$$

那么，作为向量的坐标（称为逆变坐标）和作为余向量的坐标（称为协变坐标）有什么联系呢？我们看$\forall b \in V,$成立

$$
(a, b) = a_jb^j = g_ija^ib^j,
$$

> 注14：不过标准正交基下gij = δij又与δji 不一样。

于是应有

$$
a_j = g_ija^i,
$$

用向量坐标给出了余向量坐标，称为降指标。

可以这样来理解向量与余向量。同一个向量，如果在基{e<sub>1</sub>, · · ·, e<sub>n</sub>}下展开（看向量在这组基上的投影），它就是一个普通意义下的向量；但一个向量也可以通过其它向量（至少n个，例如上述基向量）跟它的内积来确定，这时它就看成了余向量。

反过来，记

$$
g^ij = (e^i, e^j),
$$

就可以构造余向量空间中的度量阵

$$
\tilde G = (g^ij)_n\times n,
$$

与上面一样可以知道

$$
a^i = g^ija_j,
$$

称为升指标。而且这里G, $\tilde{G}$是一对逆矩阵，

$$
\tilde G = G^{-1},
$$

分量表示为

$$
g^ijg_jk = \delta ^i_k.
$$

注意到G = (g<sub>ij</sub>)定义了双线性型（内积）

$$
G(a, b) = g_ija^ib^j,
$$

从而G $\in V^{*}$$\otimes V^{*}$.类似地，G<sup>-1</sup> = (g<sup>ij</sup>) $\in$V $\otimes$V.因此，也称度量阵为度量张量。

张量的加法和数乘前面已定义，而且，如$V^{*}$$\otimes V^{*} \otimes$V 在这样的加法和数乘下本身构成了一个线性空间。

对于两个张量，譬如

$$
f = f^{\mathsf{T}}k_ije^i ⊗e^j ⊗e_k \in V^{*}⊗V^{*}⊗V
$$

和

$$
g = g^qr_p e^p ⊗e_q ⊗e_r \in V^{*}⊗V ⊗V,
$$

它们的张量积定义为15

$$
(f ⊗g)(u, v, w; x, y, z) = f(u, v; x)g(w; y, z),
$$

> 注15：以下变量之间用分号隔开V 中向量和V $\ast$中的泛函。

其中$\forall u, v, w \in V, x, y, z \in V^{*}$.

容易看出，f $\otimes$g 仍然保持多线性性质，这使它成为张量，而且是在张量空间$V^{*}$$\otimes V^{*}$$\otimes V^{*} \otimes$V $\otimes$V $\otimes$V 中。<sup>16</sup>

进一步有

f $\otimes$g = f<sup>k</sup><sub>ij</sub>g<sup>qr</sup><sub>p</sub> e<sup>i</sup> $\otimes$e<sup>j</sup> $\otimes$e<sup>p</sup> $\otimes$e<sub>k</sub> $\otimes$e<sub>q</sub> $\otimes$e<sub>r</sub>,

这就给出了张量积的坐标

$$
(f ⊗g)^kqr_ijp = f^ k_ijg^qr_p.
$$

对于f $\in V^{*}$$\otimes V^{*}$$\otimes V^{*} \otimes$V $\otimes$V,选定两个数字如2和1,在f的自变量中，固定其中相应序号为2的向量为e<sub>j</sub>,以及序号为1的泛函为ej 并求和，得到的张量称为f 按逆变2-指标和协变1-指标的卷积，记为

$$
tr^1_2(f) \in V^{*}⊗V^{*}⊗V,
$$

定义如tr<sup>1</sup><sub>2</sub>(f)(u, w; y)

$$
=f(u, e_s, w; e^s, y).
$$

其多线性性显然，故仍为张量。表示为

$$
tr^1_2(f) = tr^1_2(f)^m_ike^i ⊗e^k ⊗e_m,
$$

其中

$$
tr^1_2(f)^m_ik = f^ jm_ijk.
$$

线性变换（即(1, 1)型张量）A的迹就可以用卷积来表述：

$$
tr^1_1A = A^ℓ_ℓ.
$$

张量如f $\in V^{*}$$\otimes V^{*} \otimes$V, g $\in V^{*} \otimes$V $\otimes$V 的点积定义为

$$
f \cdot g=tr^1_3(f ⊗g)
 f^{\mathsf{T}}k_ijg^mne^i ⊗e^j ⊗e_m ⊗e_n,
 =
$$

k也就是说，“消去”了f的最后一个逆变变量和g的第一个协变变量，于是

(f · g)(u, v; y, z)

$$
=f(u, v; e^k)g(e_k; y, z).
$$

类似地还可以定义张量的双点积、三点积等。

> 注16：这个张量空间包含所有形如f $\otimes$g这样的张量，以及它们的线性组合。

以下讨论3阶协变张量f $\in V^{*}$$\otimes V^{*}$$\otimes V^{*}$.如上所述，f的定义为

$$
f(u, v, w) = f_ijku^iv^jw^k,
$$

即

$$
f = f_ijke^i ⊗e^j ⊗e^k.
$$

定义(1, 2, 3)的一个排列

$$
\pi = (\pi _1, \pi _2, \pi _3),
$$

按照这个排列改变(u, v, w)的顺序，例如$\pi$ = (3, 1, 2)时，

$$
\pi (u, v, w) = (w, u, v).
$$

利用这样的$\pi$改变f如下：

$$
(f ◦\pi)(u, v, w) = f(\pi (u, v, w)).
$$

把所有的排列都取到，并且求张量的”平均值”给出了对称化张量。例如，

$$
\begin{aligned}
\operatorname{Sym}(e^1 \otimes e^2 \otimes e^2) &= \tfrac{1}{3}(e^1 \otimes e^2 \otimes e^2 + e^2 \otimes e^1 \otimes e^2 + e^2 \otimes e^2 \otimes e^1), \\
\operatorname{Sym}(e^1 \otimes e^2 \otimes e^3) &= \tfrac{1}{6}(e^1 \otimes e^2 \otimes e^3 + e^1 \otimes e^3 \otimes e^2 + e^2 \otimes e^1 \otimes e^3 \\
&\quad + e^2 \otimes e^3 \otimes e^1 + e^3 \otimes e^1 \otimes e^2 + e^3 \otimes e^2 \otimes e^1).
\end{aligned}
$$

在行列式引入时，排列$\pi$的逆序数定义为$\tau$<sub>$\pi$</sub> = $\tau$($\pi$<sub>1</sub>, $\pi$<sub>2</sub>, $\pi$<sub>3</sub>).

称f反对称，若$\forall$$\pi$,

$$
f ◦\pi = \tau _\pi f,
$$

即

$$
f(\pi (u, v, w)) = \tau _\pi f(u, v, w).
$$

按照$\tau$<sub>$\pi$</sub> = $\pm$1可把所有排列分成两个子集，它们之间相差一个对换。

称Asym(f) $\equiv$ (1/3!) $\sum$ $\tau$<sub>$\pi$</sub> f ◦ $\pi$ 为f $\in V^{*}$$\otimes V^{*}$$\otimes V^{*}$ 的交错化张量，其中$\pi$取遍所有排列。

容易验证，交错化张量反对称。

**定义9.7.** 若p阶协变张量f反对称，称为V 上的一个p次外形式。所有V 上的p次外形式构成的集合记为$\Lambda$<sup>p</sup>($V^{*}$).

外形式对于加法和数乘封闭，因此$\Lambda$<sup>p</sup>($V^{*}$)是p阶协变张量空间的一个子空间。

同理，若q阶逆变张量反对称，称为V 上的一个q向量，所有的q向量构成的集合记为$\Lambda$<sup>q</sup>(V )，是q阶逆变张量空间的一个子空间。

**定义9.8.** v<sub>1</sub>, · · ·, v<sub>q</sub> $\in$V ，定义它们的外积

$$
v_1 ∧\cdots ∧v_q = Asym(v_1 ⊗\cdots ⊗v_q).
$$

V 中选择一组基{e<sub>1</sub>, · · ·, e<sub>n</sub>},那么

$$
e_i1 ∧\cdots ∧e_iq,1 ≤i_1 < \cdots < i_q ≤n,
$$

可以线性表出$\Lambda$<sup>q</sup>(V )中所有张量。还可以证明，这些外积得到的向量线性无关，从而构成子空间$\Lambda$<sup>q</sup>(V )的一组基。特别地，所有q > n的$\Lambda$<sup>q</sup>(V ) = {0}为平凡子空间。

**定理9.7.** v<sub>1</sub>, · · ·, v<sub>q</sub> $\in$V 线性无关的充要条件是v<sub>1</sub> $\wedge$· · · $\wedge$v<sub>q</sub> $\ne$  0.

证明. 若v<sub>1</sub>, · · ·, v<sub>q</sub> $\in$V 线性相关，其中至少一个向量可表示为前面向量的线性组合。不妨

$$
v_q = \sum_{i=1}^{q-1} \lambda_i v_i,
$$

那么

$$
v_1 \wedge \cdots \wedge v_q = \sum_{i=1}^{q-1} \lambda_i v_1 \wedge \cdots \wedge v_{q-1} \wedge v_i.
$$

右式每一项都有重复的向量，由反对称性可证外积为0.故

$$
v_1 ∧\cdots ∧v_q = 0.
$$

另一方面，如果v<sub>1</sub>, · · ·, v<sub>q</sub> $\in$V 线性无关，那么它们可以扩张成V 的一组（n个向量构成的）基，从而v<sub>1</sub> $\wedge$· · · $\wedge$v<sub>q</sub> 是$\Lambda$<sup>q</sup>(V ) 的一个基向量，当然非0.    ■

现在，设任一组向量v<sub>1</sub>, · · ·, v<sub>n</sub>的坐标表示为

$$
v_j = a^i_je_i,
$$

那么v<sub>1</sub> $\wedge$· · · $\wedge$v<sub>n</sub>可在$\Lambda$<sup>n</sup>(V )的基上展开，然而$\Lambda$<sup>n</sup>(V )中只有一个基向量e<sub>1</sub> $\wedge$ · · · $\wedge$e<sub>n</sub>,于是

$$
v_1 ∧\cdots ∧v_n = \Delta e_1 ∧\cdots ∧e_n.
$$

这里的$\triangle$$\in \mathbb{R}$可以证明关于v<sub>i</sub>具有多线性性,对换后变号，且当v<sub>i</sub> = e<sub>i</sub>即

$$
a^i_j = \delta ^i
$$

j时$\triangle$= 1.因此，

$$
\Delta = det(a^i_j).
$$

这就是行列式的又一种由来。重积分坐标变换时的雅可比行列式也是同一个来历。

## 9.5 作业

1. 函数sin x, cos x都在某线性空间V 中。

(a) 若定义 f($\phi$(x)) = $\sqrt$2$\cdot$$\phi$(3$\pi$/4)，试验证它确是线性泛函，且满足 f(sin x) = 1, f(cos x) = -1；并求 f(sin(x + $\pi$/6));

(b) 若定义 g($\phi$(x)) = $\int$_{$\pi$/2}^{$\pi$} $\phi$(x)dx，试验证它亦是线性泛函，且满足 g(sin x) = 1, g(cos x) = -1；并求 g(sin(x + $\pi$/6));

(c) 上述二者是否相等？若是，f(tan x)与g(tan x)是否相等？为何如此？

2. f是n维内积空间V 上的线性泛函，求证：

(a) 必存在V 的一组标准正交基{$\varepsilon$<sub>1</sub>, · · ·, $\varepsilon$<sub>n</sub>},成立：$\forall v \in V, f(v) = v_{1},$这里v<sub>1</sub> 是v在标准正交基下的第一个坐标;

(b) f在V 上点点连续；

(c) 若 f 不恒取零值，定义 \|f\| = sup_{v$\ne$0} |f(v)|/\|v\|, d = inf{\|v\| | f(v) = 1}，则 \|f\|d = 1.

3. f<sub>1</sub>, · · ·, f<sub>r</sub>是n维线性空间V 上的线性泛函。求证：

(a) W = {v $\in$V |f<sub>1</sub>(v) = · · · = f<sub>r</sub>(v) = 0}是V 的子空间；

(b) 若W为V 的一个子空间，则必存在若干个线性泛函f<sub>1</sub>, · · ·, f<sub>r</sub> (r $\le$

$$
n),使得W = {v \in V |f_1(v) = \cdots = f_r(v) = 0}.
$$

4. 在空间$\mathbb{R}^3$中取一组基$\varepsilon$<sub>1</sub> = (1, 1, 1), $\varepsilon$<sub>2</sub> = (1, 1, -1), $\varepsilon$<sub>3</sub> = (1, -1, -1),而{$\varepsilon$<sup>1</sup>, $\varepsilon$<sup>2</sup>, $\varepsilon$<sup>3</sup>}是其对偶基，试对x = (1, 0, 0)求：$\varepsilon$<sup>1</sup>(x), $\varepsilon$<sup>2</sup>(x), $\varepsilon$<sup>3</sup>(x).

5. 考虑$\mathbb{R}^3$中的基

$$
\varepsilon _1 = (1, 0, 0)^T, \varepsilon _2 = (1, 1, 0)^T, \varepsilon _3 = (1, 1, 1)^T,
$$

向量a在这组基下坐标向量为$x = (1, 2, 3)^\mathsf{T}$;计算对偶基，若此对偶基下某线性泛函f的坐标向量为$y = (2, 3, 4)^\mathsf{T}$, 求f(a).另选一组基

$$
\tilde \varepsilon _1 = (0, 0, 1)^T, \tilde \varepsilon _2 = (0, 1, 1)^T, \tilde \varepsilon _3 = (1, 0, 1)^T,
$$

求a在这组基下的坐标向量$\tilde{x}$和对偶基{$\tilde{varepsilon}$<sup>1</sup>, $\tilde{varepsilon}$<sup>2</sup>, $\tilde{varepsilon}$<sup>3</sup>},以及f新的坐标向量；并计算f(a).

6. 在多项式空间P<sub>2</sub>[x]上，分别求以下两种基的对偶基：

(a) {$\varepsilon$<sub>0</sub>, $\varepsilon$<sub>1</sub>, $\varepsilon$<sub>2</sub>}为一组基，其中$\varepsilon$<sub>j</sub> = [x]<sup>j</sup>；

(b) 先取定3个不同的实数x<sub>0</sub> = -1, x<sub>1</sub> = 0, x<sub>2</sub> = 1,写出拉格朗日插值多项式例子中的基{$\tilde{varepsilon}$<sub>0</sub>, $\tilde{varepsilon}$<sub>1</sub>, $\tilde{varepsilon}$<sub>2</sub>}.

若给定函数

$$
p([x]) = 4[x]^2 -4[x] + 1,
$$

请求出它在两组基下的坐标。计算线性泛函

$$
f(1) = -2, f([x]) = 1, f([x]^2) = 2,
$$

在两组对偶基下的坐标向量，以及在p([x])上的取值。

7. f(a, b)为$\mathbb{R}^3$上的双线性型，若已知：

f([1, 0, 0]$^\mathsf{T}$, [1, 1, 1]$^\mathsf{T}$) = 3;f([1, 0, 0]$^\mathsf{T}$, [1, 1, 0]$^\mathsf{T}$) = 2; f([1, 0, 0]$^\mathsf{T}$, [1, 0, 0]$^\mathsf{T}$) = 1;f([0, 1, 0]$^\mathsf{T}$, [1, 1, 0]$^\mathsf{T}$) = 2; f([0, 1, 0]$^\mathsf{T}$, [1, 0, 1]$^\mathsf{T}$) = 1;f([0, 1, 0]$^\mathsf{T}$, [0, 0, 1]$^\mathsf{T}$) = 0; f([0, 0, 1]$^\mathsf{T}$, [1, 0, 0]$^\mathsf{T}$) = 1;f([0, 0, 1]$^\mathsf{T}$, [0, 1, 0]$^\mathsf{T}$) = 1; f([0, 0, 1]$^\mathsf{T}$, [0, 0, 1]$^\mathsf{T}$) = 1;

求f(a, b)的度量阵，以及f([1, 2, 3]$^\mathsf{T}$, [3, 2, 1]$^\mathsf{T}$).

8. 在n维内积空间V 上，求证：双线性型f(a, b)为对称的，当且仅当V 有一组标准正交基，使f(a, b)的度量阵为对角阵。

9. 求证n维内积空间V 上有同构$L(V \times V, \mathbb{R})$ = $\mathbb{R}^{n \times n}$.